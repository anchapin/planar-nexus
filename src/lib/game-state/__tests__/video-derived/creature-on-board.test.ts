/**
 * Video-Derived Fixture: creature-on-board
 * Description: Single creature in play
 * Fixture ID: creature-on-board
 * Source: video-derived (issue #1397 — see docs/TEST_VIDEO_FIXTURES.md)
 *
 * Auto-generated by scripts/regen-video-fixtures.mjs (issue #1397).
 * Each "validates behavior" it-block now delegates to validateBehavior()
 * for a typed assertion against the RecognizedBoardState.
 */

import { validateBehavior } from "./_helpers";

const creature_on_board = {
  id: "creature-on-board",
  name: "creature-on-board",
  description: "Single creature in play",
  gameState: {
    player_life: 20,
    opponent_life: 20,
    battlefield_player: [
      {
        name: "Grizzly Bears",
        is_tapped: false,
        power: 2,
        toughness: 2,
      },
      {
        name: "Forest",
        is_tapped: false,
        power: 0,
        toughness: 0,
      },
    ],
    battlefield_opponent: [],
    hand_size: 5,
    graveyard: [],
    stack: [],
    phase: "main",
    turn_number: 2,
  },
  expectedBehaviors: [
    "Game state should serialize without errors",
    "Player life totals should be valid integers",
    "Battlefield arrays should contain valid card entries",
    "Hand size should be a non-negative integer",
    "Phase should be a recognized game phase string",
    "Turn number should be a positive integer",
    "Graveyard should contain card name strings",
    "Stack should contain spell name strings or be empty",
  ],
};

describe("Video-Derived Fixture: creature-on-board", () => {
  it("loads game state successfully", () => {
    expect(typeof creature_on_board.gameState).toBe("object");
    expect(creature_on_board.gameState).not.toBeNull();
  });

  it("has valid player data", () => {
    expect(Number.isInteger(creature_on_board.gameState.player_life)).toBe(
      true,
    );
    expect(Number.isInteger(creature_on_board.gameState.opponent_life)).toBe(
      true,
    );
    expect(Array.isArray(creature_on_board.gameState.battlefield_player)).toBe(
      true,
    );
    expect(
      Array.isArray(creature_on_board.gameState.battlefield_opponent),
    ).toBe(true);
  });

  it("has valid turn structure", () => {
    expect(typeof creature_on_board.gameState.turn_number).toBe("number");
    expect(Number.isInteger(creature_on_board.gameState.turn_number)).toBe(
      true,
    );
    expect(creature_on_board.gameState.turn_number).toBeGreaterThan(0);
    expect(typeof creature_on_board.gameState.phase).toBe("string");
    expect(creature_on_board.gameState.phase.length).toBeGreaterThan(0);
  });

  it("can be serialized and deserialized", () => {
    const serialized = JSON.stringify(creature_on_board.gameState);
    const deserialized = JSON.parse(serialized);
    expect(deserialized).toEqual(creature_on_board.gameState);
  });

  for (const behavior of creature_on_board.expectedBehaviors) {
    it(`validates behavior: ${behavior}`, () => {
      // Real assertion: see _helpers/validate-fixture.ts for the
      // behaviour-string → typed-property mapping. Issue #1397 replaces
      // the previous TODO placeholder with a concrete property check.
      validateBehavior(behavior, creature_on_board.gameState);
    });
  }
});
