/**
 * Video-Derived Fixture: hand-size-tracking
 * Description: Various hand sizes
 * Fixture ID: hand-size-tracking
 * Source: video-derived (issue #1397 — see docs/TEST_VIDEO_FIXTURES.md)
 *
 * Auto-generated by scripts/regen-video-fixtures.mjs (issue #1397).
 * Each "validates behavior" it-block now delegates to validateBehavior()
 * for a typed assertion against the RecognizedBoardState.
 */

import { validateBehavior } from "./_helpers";

const hand_size_tracking = {
  id: "hand-size-tracking",
  name: "hand-size-tracking",
  description: "Various hand sizes",
  gameState: {
    player_life: 20,
    opponent_life: 20,
    battlefield_player: [
      {
        name: "Island",
        is_tapped: true,
        power: 0,
        toughness: 0,
      },
      {
        name: "Island",
        is_tapped: true,
        power: 0,
        toughness: 0,
      },
    ],
    battlefield_opponent: [
      {
        name: "Mountain",
        is_tapped: false,
        power: 0,
        toughness: 0,
      },
    ],
    hand_size: 4,
    graveyard: [],
    stack: [],
    phase: "main",
    turn_number: 3,
  },
  expectedBehaviors: [
    "Game state should serialize without errors",
    "Player life totals should be valid integers",
    "Battlefield arrays should contain valid card entries",
    "Hand size should be a non-negative integer",
    "Phase should be a recognized game phase string",
    "Turn number should be a positive integer",
    "Graveyard should contain card name strings",
    "Stack should contain spell name strings or be empty",
  ],
};

describe("Video-Derived Fixture: hand-size-tracking", () => {
  it("loads game state successfully", () => {
    expect(typeof hand_size_tracking.gameState).toBe("object");
    expect(hand_size_tracking.gameState).not.toBeNull();
  });

  it("has valid player data", () => {
    expect(Number.isInteger(hand_size_tracking.gameState.player_life)).toBe(
      true,
    );
    expect(Number.isInteger(hand_size_tracking.gameState.opponent_life)).toBe(
      true,
    );
    expect(Array.isArray(hand_size_tracking.gameState.battlefield_player)).toBe(
      true,
    );
    expect(
      Array.isArray(hand_size_tracking.gameState.battlefield_opponent),
    ).toBe(true);
  });

  it("has valid turn structure", () => {
    expect(typeof hand_size_tracking.gameState.turn_number).toBe("number");
    expect(Number.isInteger(hand_size_tracking.gameState.turn_number)).toBe(
      true,
    );
    expect(hand_size_tracking.gameState.turn_number).toBeGreaterThan(0);
    expect(typeof hand_size_tracking.gameState.phase).toBe("string");
    expect(hand_size_tracking.gameState.phase.length).toBeGreaterThan(0);
  });

  it("can be serialized and deserialized", () => {
    const serialized = JSON.stringify(hand_size_tracking.gameState);
    const deserialized = JSON.parse(serialized);
    expect(deserialized).toEqual(hand_size_tracking.gameState);
  });

  for (const behavior of hand_size_tracking.expectedBehaviors) {
    it(`validates behavior: ${behavior}`, () => {
      // Real assertion: see _helpers/validate-fixture.ts for the
      // behaviour-string → typed-property mapping. Issue #1397 replaces
      // the previous TODO placeholder with a concrete property check.
      validateBehavior(behavior, hand_size_tracking.gameState);
    });
  }
});
