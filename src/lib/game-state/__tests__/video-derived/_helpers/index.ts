/**
 * Video-Derived Fixture Helpers — barrel export
 *
 * Public surface used by the regenerated `*.test.ts` files in this directory.
 */

export {
  RECOGNIZED_PHASES,
  isRecognizedPhase,
  validateBehavior,
  validateAllBehaviors,
  extractCardName,
  type ValidationResult,
} from "./validate-fixture";
