/**
 * Video-Derived Fixture: multi-land-board
 * Description: Multiple lands in play
 * Fixture ID: multi-land-board
 * Source: video-derived (issue #1397 — see docs/TEST_VIDEO_FIXTURES.md)
 *
 * Auto-generated by scripts/regen-video-fixtures.mjs (issue #1397).
 * Each "validates behavior" it-block now delegates to validateBehavior()
 * for a typed assertion against the RecognizedBoardState.
 */

import { validateBehavior } from "./_helpers";

const multi_land_board = {
  id: "multi-land-board",
  name: "multi-land-board",
  description: "Multiple lands in play",
  gameState: {
    player_life: 20,
    opponent_life: 20,
    battlefield_player: [
      {
        name: "Island",
        is_tapped: false,
        power: 0,
        toughness: 0,
      },
      {
        name: "Island",
        is_tapped: false,
        power: 0,
        toughness: 0,
      },
      {
        name: "Volcanic Island",
        is_tapped: false,
        power: 0,
        toughness: 0,
      },
    ],
    battlefield_opponent: [
      {
        name: "Mountain",
        is_tapped: false,
        power: 0,
        toughness: 0,
      },
      {
        name: "Swamp",
        is_tapped: false,
        power: 0,
        toughness: 0,
      },
    ],
    hand_size: 6,
    graveyard: [],
    stack: [],
    phase: "main",
    turn_number: 4,
  },
  expectedBehaviors: [
    "Game state should serialize without errors",
    "Player life totals should be valid integers",
    "Battlefield arrays should contain valid card entries",
    "Hand size should be a non-negative integer",
    "Phase should be a recognized game phase string",
    "Turn number should be a positive integer",
    "Graveyard should contain card name strings",
    "Stack should contain spell name strings or be empty",
  ],
};

describe("Video-Derived Fixture: multi-land-board", () => {
  it("loads game state successfully", () => {
    expect(typeof multi_land_board.gameState).toBe("object");
    expect(multi_land_board.gameState).not.toBeNull();
  });

  it("has valid player data", () => {
    expect(Number.isInteger(multi_land_board.gameState.player_life)).toBe(true);
    expect(Number.isInteger(multi_land_board.gameState.opponent_life)).toBe(
      true,
    );
    expect(Array.isArray(multi_land_board.gameState.battlefield_player)).toBe(
      true,
    );
    expect(Array.isArray(multi_land_board.gameState.battlefield_opponent)).toBe(
      true,
    );
  });

  it("has valid turn structure", () => {
    expect(typeof multi_land_board.gameState.turn_number).toBe("number");
    expect(Number.isInteger(multi_land_board.gameState.turn_number)).toBe(true);
    expect(multi_land_board.gameState.turn_number).toBeGreaterThan(0);
    expect(typeof multi_land_board.gameState.phase).toBe("string");
    expect(multi_land_board.gameState.phase.length).toBeGreaterThan(0);
  });

  it("can be serialized and deserialized", () => {
    const serialized = JSON.stringify(multi_land_board.gameState);
    const deserialized = JSON.parse(serialized);
    expect(deserialized).toEqual(multi_land_board.gameState);
  });

  for (const behavior of multi_land_board.expectedBehaviors) {
    it(`validates behavior: ${behavior}`, () => {
      // Real assertion: see _helpers/validate-fixture.ts for the
      // behaviour-string → typed-property mapping. Issue #1397 replaces
      // the previous TODO placeholder with a concrete property check.
      validateBehavior(behavior, multi_land_board.gameState);
    });
  }
});
