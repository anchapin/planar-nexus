---
phase: 21-pre-commit-hooks
plan: "04"
type: execute
wave: 2
depends_on: ["21-03"]
files_modified: [.husky/pre-commit]
autonomous: true
requirements: [REQ-001]
user_setup: []

must_haves:
  truths:
    - "Pre-commit hooks run successfully on test commit"
    - "ESLint, Prettier, and TypeScript checks execute"
    - "Hooks work with git commit command"
  artifacts:
    - path: ".husky/pre-commit"
      contains: "lint-staged"
---

<objective>
Test pre-commit hooks locally to verify they work correctly.

Purpose: Ensure the pre-commit hooks are properly configured and functional before considering the phase complete.
Output: Verified working pre-commit hooks.
</objective>

<context>
@.planning/PROJECT.md
@.planning/milestones/v1.6-ROADMAP.md
@.planning/phases/21-pre-commit-hooks/CONTEXT.md
@.planning/phases/21-pre-commit-hooks/21-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run prepare script to ensure Husky is set up</name>
  <files>package.json</files>
  <action>
1. Run npm run prepare or npx husky install to ensure hooks are set up
2. This installs the Husky hooks in .git/hooks
  </action>
  <verify>ls -la .git/hooks/ | grep husky</verify>
  <done>Husky hooks installed in .git/hooks</done>
</task>

<task type="auto">
  <name>Task 2: Test pre-commit hook with a staged file</name>
  <files>.husky/pre-commit</files>
  <action>
1. Create a test file to commit: echo "test" > test-hook.txt
2. Stage the file: git add test-hook.txt
3. Try to commit: git commit -m "test: testing pre-commit hook" --no-verify (to test manually first)
4. Run the hook manually: npx lint-staged
5. If all checks pass, the hooks are working
6. Clean up: rm test-hook.txt
  </action>
  <verify>npx lint-staged --verbose 2>&1 | head -20</verify>
  <done>Pre-commit hook executes successfully</done>
</task>

<task type="auto">
  <name>Task 3: Verify complete pre-commit workflow</name>
  <files>.husky/pre-commit</files>
  <action>
1. Make a small change to an existing TypeScript file
2. Stage it: git add <file>
3. Attempt commit - verify the hook runs
4. Should see ESLint, TypeScript, and Prettier output
  </action>
  <verify>Manual verification - hook runs without errors</verify>
  <done>Complete pre-commit workflow verified</done>
</task>

</tasks>

<verification>
- [ ] Husky hooks installed in .git/hooks
- [ ] lint-staged runs on staged files
- [ ] ESLint, TypeScript, and Prettier all execute
- [ ] No errors in the hook output
</verification>

<success_criteria>

- Pre-commit hooks run successfully on test commit
- All quality checks (ESLint, TypeScript, Prettier) execute
- Hooks properly prevent commits with errors
  </success_criteria>

<output>
After completion, create `.planning/phases/21-pre-commit-hooks/21-04-SUMMARY.md`
</output>
