---
phase: 21-pre-commit-hooks
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: [package.json, .husky/pre-commit, lint-staged.config.js]
autonomous: true
requirements: [REQ-001]
user_setup: []

must_haves:
  truths:
    - "Husky git hooks are installed and active"
    - "lint-staged runs on staged files"
    - "pre-commit hook triggers on git commit"
  artifacts:
    - path: "package.json"
      contains: "husky"
    - path: ".husky/pre-commit"
      contains: "lint-staged"
    - path: "lint-staged.config.js"
      provides: "lint-staged configuration"
---

<objective>
Install and configure Husky git hooks and lint-staged for staged file processing.

Purpose: Establish the foundation for pre-commit quality checks by setting up Husky and lint-staged.
Output: Husky installed, .husky directory created, lint-staged configured.
</objective>

<context>
@.planning/PROJECT.md
@.planning/milestones/v1.6-ROADMAP.md
@.planning/phases/21-pre-commit-hooks/CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Husky and lint-staged dependencies</name>
  <files>package.json</files>
  <action>
1. Install husky as dev dependency: `npm install -D husky`
2. Install lint-staged as dev dependency: `npm install -D lint-staged`
3. Initialize Husky: `npx husky init`
4. This creates .husky directory and updates package.json
  </action>
  <verify>ls -la .husky/ && cat package.json | grep -E "husky|lint-staged"</verify>
  <done>Husky and lint-staged installed, .husky directory exists</done>
</task>

<task type="auto">
  <name>Task 2: Configure lint-staged</name>
  <files>lint-staged.config.js, package.json</files>
  <action>
1. Create lint-staged.config.js with configuration for TypeScript and other files:
```javascript
module.exports = {
  '*.{ts,tsx,js,jsx}': ['eslint --fix', 'prettier --write'],
  '*.{json,md,css}': ['prettier --write'],
};
```
2. Update package.json scripts to include prepare for Husky:
```json
"scripts": {
  "prepare": "husky install"
}
```
  </action>
  <verify>cat lint-staged.config.js && cat package.json | grep prepare</verify>
  <done>lint-staged.config.js created with proper configuration</done>
</task>

<task type="auto">
  <name>Task 3: Configure Husky pre-commit hook</name>
  <files>.husky/pre-commit</files>
  <action>
1. Edit .husky/pre-commit to run lint-staged:
```bash
npx lint-staged
```
2. Make sure the file is executable
  </action>
  <verify>cat .husky/pre-commit</verify>
  <done>.husky/pre-commit runs lint-staged on commit</done>
</task>

</tasks>

<verification>
- [ ] npm install completes without errors
- [ ] .husky directory exists with pre-commit hook
- [ ] lint-staged.config.js exists
- [ ] package.json has husky and lint-staged dependencies
</verification>

<success_criteria>

- Husky is initialized and .husky directory exists
- lint-staged is configured to run on staged files
- pre-commit hook is set up to trigger lint-staged
  </success_criteria>

<output>
After completion, create `.planning/phases/21-pre-commit-hooks/21-01-SUMMARY.md`
</output>
