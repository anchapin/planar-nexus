---
phase: 24-integration-tests
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: ["REQ-004"]
user_setup: []

# Goal-backward verification
must_haves:
  truths:
    - "MSW is configured for API mocking"
    - "Integration test patterns exist for server actions"
    - "Integration tests cover deck creation/saving"
    - "Integration tests cover card search/filtering"
    - "Integration tests cover AI coach interactions"
  artifacts:
    - path: "src/test-utils/msw/handlers.ts"
      provides: "MSW handlers for API mocking (already exists)"
      min_lines: 100
    - path: "src/test-utils/integration.ts"
      provides: "Integration test utilities and patterns (already exists)"
      min_lines: 80
    - path: "src/test-utils/integration/"
      provides: "Integration test directory"
    - path: "src/test-utils/integration/deck.spec.ts"
      provides: "Deck creation/saving integration tests"
    - path: "src/test-utils/integration/card-search.spec.ts"
      provides: "Card search/filtering integration tests"
    - path: "src/test-utils/integration/ai-coach.spec.ts"
      provides: "AI coach interaction tests"
  key_links:
    - from: "src/test-utils/integration/"
      to: "src/test-utils/msw/"
      via: "MSW handlers used in integration tests"
    - from: "package.json"
      to: "src/test-utils/msw/"
      via: "MSW dependency"
      pattern: "msw"
---

<objective>
Verify existing MSW setup and create integration tests for critical user flows.

Purpose: Complete REQ-004 integration test requirements.
Output: Verified MSW setup + integration tests for deck creation, card search, and AI coach.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v1.6-REQUIREMENTS.md
@src/test-utils/index.ts
@src/test-utils/integration.ts
@src/test-utils/msw/handlers.ts
</context>

<tasks>

<task type="auto">
  <name>Verify MSW is installed</name>
  <files>package.json</files>
  <action>Verify MSW is already installed:
- Check npm list msw
- Check for mockServiceWorker.js in public/ directory

MSW is likely already installed from Phase 23.</action>
<verify>npm list msw 2>/dev/null && ls -la public/mockServiceWorker.js 2>/dev/null || echo "MSW not fully initialized"</verify>
<done>MSW is installed and service worker exists</done>
</task>

<task type="auto">
  <name>Verify existing MSW handlers</name>
  <files>src/test-utils/msw/handlers.ts</files>
  <action>Verify existing MSW handlers in src/test-utils/msw/handlers.ts:
- Check Scryfall API handlers exist
- Check createMockScryfallCard function exists
- Check handlers are properly exported</action>
  <verify>cat src/test-utils/msw/handlers.ts | grep -E "(export|handler)" | head -10</verify>
  <done>MSW handlers exist and are properly structured</done>
</task>

<task type="auto">
  <name>Verify existing integration utilities</name>
  <files>src/test-utils/integration.ts</files>
  <action>Verify existing integration utilities:
- Check createMockDeck function
- Check createMockCard function  
- Check MSW server utilities

These exist from Phase 23.</action>
<verify>cat src/test-utils/integration.ts | grep -E "export (function|const)" | head -10</verify>
<done>Integration utilities exist with mock helpers</done>
</task>

<task type="auto">
  <name>Initialize MSW service worker</name>
  <files>public/mockServiceWorker.js</files>
  <action>If not already initialized, run:
- npx msw init public/ --save

This creates the service worker file needed for browser mocking.</action>
<verify>ls -la public/mockServiceWorker.js</verify>
<done>Service worker file exists in public/</done>
</task>

<task type="auto">
  <name>Create integration tests directory</name>
  <files>src/test-utils/integration/</files>
  <action>Create integration test directory:
- mkdir -p src/test-utils/integration

This is where the actual integration test specs will go.</action>
<verify>ls -la src/test-utils/integration/</verify>
<done>Integration test directory exists</done>
</task>

<task type="auto">
  <name>Write deck creation integration tests</name>
  <files>src/test-utils/integration/deck.spec.ts</files>
  <action>Create integration tests for deck creation and saving:
- Test creating a new deck
- Test adding cards to deck
- Test saving deck to local storage
- Test deck validation

Use createMockDeck from @/test-utils/integration and test using Jest.</action>
<verify>cat src/test-utils/integration/deck.spec.ts | grep -E "(test|it)\(" | head -5</verify>
<done>At least 3 test cases for deck operations exist</done>
</task>

<task type="auto">
  <name>Write card search integration tests</name>
  <files>src/test-utils/integration/card-search.spec.ts</files>
  <action>Create integration tests for card search and filtering:
- Test basic card search
- Test filter by color/type/cmc
- Test sorting results

Use MSW handlers from @/test-utils/msw/handlers for Scryfall API mocking.</action>
<verify>cat src/test-utils/integration/card-search.spec.ts | grep -E "(test|it)\(" | head -5</verify>
<done>At least 3 test cases for card search exist</done>
</task>

<task type="auto">
  <name>Write AI coach integration tests</name>
  <files>src/test-utils/integration/ai-coach.spec.ts</files>
  <action>Create integration tests for AI coach interactions:
- Test AI deck review request
- Test AI suggestions display

Mock the AI service responses.</action>
<verify>cat src/test-utils/integration/ai-coach.spec.ts | grep -E "(test|it)\(" | head -5</verify>
<done>At least 2 test cases for AI coach exist</done>
</task>

<task type="auto">
  <name>Verify integration tests run</name>
  <files>package.json</files>
  <action>Verify the integration tests can run:
- npm test -- --testPathPattern="integration" --passWithNoTests
- Check tests pass or at least execute</action>
  <verify>npm test -- --testPathPattern="integration" --passWithNoTests 2>&1 | tail -20</verify>
  <done>Integration tests execute without errors</done>
</task>

</tasks>

<verification>
- [ ] MSW is installed (npm list msw)
- [ ] MSW service worker exists (public/mockServiceWorker.js)
- [ ] MSW handlers exist in src/test-utils/msw/handlers.ts
- [ ] Integration utilities exist in src/test-utils/integration.ts
- [ ] Integration tests exist for deck, card-search, and AI coach
- [ ] Tests can execute via npm test
</verification>

<success_criteria>

- All tasks completed
- MSW is configured for API mocking
- Integration test patterns exist for server actions
- Integration tests cover deck creation, card search, and AI coach
- REQ-004 requirements fully addressed
  </success_criteria>

<output>
After completion, create `.planning/phases/24-integration-tests/24-01-SUMMARY.md`
</output>
