---
phase: 25-ci-quality-gates
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  [
    ".github/workflows/ci.yml",
    "package.json",
    "commitlint.config.js",
    "docs/BRANCH_PROTECTION.md",
  ]
autonomous: true
requirements: [REQ-005]
user_setup: []

must_haves:
  truths:
    - "Coverage threshold gate already exists in CI (verified from Phase 22)"
    - "Commit message linting enforces conventional commits format"
    - "npm audit security scan runs on every CI run"
    - "Branch protection recommendations documented"
  artifacts:
    - path: "package.json"
      contains: "commitlint"
    - path: "commitlint.config.js"
      provides: "commit message linting rules"
    - path: ".github/workflows/ci.yml"
      contains: "npm audit"
    - path: "docs/BRANCH_PROTECTION.md"
      provides: "branch protection recommendations"
---

<objective>
Configure CI quality gates to prevent regressions, enforce commit standards, and scan for security vulnerabilities.

Purpose: Establish automated quality enforcement in CI pipeline.
Output: GitHub Actions workflow updated with commit linting and security scanning.
</objective>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/milestones/v1.6-REQUIREMENTS.md
@.planning/milestones/v1.6-ROADMAP.md
@.planning/phases/25-ci-quality-gates/25-CONTEXT.md

# Check existing CI and coverage configuration

.github/workflows/ci.yml
jest.config.js
package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify coverage gate (Phase 22 complete)</name>
  <files>.github/workflows/ci.yml, jest.config.js</files>
  <action>
1. Verify existing coverage configuration in jest.config.js has thresholds:
   - Lines: 70%, Branches: 60%, Functions: 70%, Statements: 70%
2. Verify CI workflow runs: npm test -- --coverage (already present in ci.yml)
3. Jest exits non-zero when coverage is below threshold - CI will fail
4. Add documentation comment in ci.yml explaining coverage gate exists
5. Document in verification that coverage gate is already active from Phase 22
</action>
  <verify>grep -E "coverageThreshold|coverage" jest.config.js && grep "coverage" .github/workflows/ci.yml</verify>
  <done>Coverage gate verified: CI fails if below threshold (Lines 70%, Branches 60%, Functions 70%, Statements 70%)</done>
</task>

<task type="auto">
  <name>Task 2: Configure commit message linting</name>
  <files>package.json, commitlint.config.js, .github/workflows/ci.yml</files>
  <action>
1. Install commitlint and conventional config:
   - npm install --save-dev @commitlint/cli @commitlint/config-conventional
2. Create commitlint.config.js with conventionalcommits configuration:
   ```javascript
   module.exports = {
     extends: ['@commitlint/config-conventional'],
     formatter: '@commitlint/format',
     rules: {
       'header-min-length': [2, 'always', 10],
       'header-case': [2, 'always', ['lower-case']],
       'type-empty': [2, 'never'],
       'type-enum': [2, 'always', ['feat', 'fix', 'docs', 'style', 'refactor', 'test', 'chore', 'revert']]
     }
   }
   ```
3. Add commitlint lint job to GitHub Actions ci.yml workflow to check commit messages on PRs
4. Also add Husky commit-msg hook if not already present from Phase 21
</action>
  <verify>cat commitlint.config.js</verify>
  <done>Commit message linting enforces conventional commits format</done>
</task>

<task type="auto">
  <name>Task 3: Add npm audit security scanning to CI</name>
  <files>.github/workflows/ci.yml</files>
  <action>
1. Add npm audit step to GitHub Actions ci.yml workflow:
   - Add after dependency installation
   - Use: npm audit --audit-level=high
   - This fails CI on high and critical vulnerabilities
2. Consider adding npm audit --fix in a separate step to auto-fix minor issues
3. Add as separate job or combine with lint job
</action>
  <verify>grep -E "npm audit" .github/workflows/ci.yml</verify>
  <done>npm audit security scan runs in CI, fails on high/critical vulnerabilities</done>
</task>

<task type="auto">
  <name>Task 4: Document branch protection recommendations</name>
  <files>docs/BRANCH_PROTECTION.md</files>
  <action>
1. Create docs/BRANCH_PROTECTION.md with GitHub branch protection recommendations:
   - Require pull request reviews (1-2 reviewers)
   - Require status checks to pass before merge:
     - CI (test, lint, typecheck, build)
     - Coverage gate
     - Commit linting
   - Require branches to be up to date before merge
   - Prevent force pushes to main/master
   - Include admin exemptions documentation
2. Document current CI jobs that should be required:
   - test (with coverage)
   - lint
   - typecheck
   - build
</action>
  <verify>cat docs/BRANCH_PROTECTION.md</verify>
  <done>Branch protection recommendations documented in docs/BRANCH_PROTECTION.md</done>
</task>

</tasks>

<verification>
- [ ] jest.config.js has coverage thresholds (verified)
- [ ] .github/workflows/ci.yml runs coverage (verified)
- [ ] commitlint.config.js exists with conventionalcommits
- [ ] commitlint job added to ci.yml
- [ ] npm audit step added to ci.yml
- [ ] docs/BRANCH_PROTECTION.md created with recommendations
</verification>

<success_criteria>

- All 4 tasks completed
- Coverage gate verified (from Phase 22) - CI fails if below threshold
- Commit message linting enforced in CI and locally via Husky
- npm audit security scan runs on every CI run
- Branch protection recommendations available for project maintainers
  </success_criteria>

<output>
After completion, create `.planning/phases/25-ci-quality-gates/25-01-SUMMARY.md`
</output>
