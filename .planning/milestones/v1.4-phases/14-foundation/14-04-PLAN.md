---
phase: 14-foundation
plan: "04"
type: execute
wave: 2
depends_on:
  - "14-03"
files_modified:
  - e2e/sealed-mode.spec.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "Set browser allows browsing and selecting sets"
    - "Sealed pool generates with 84 cards"
    - "Pool filtering works for color, type, and CMC"
    - "Limited deck builder enforces 40-card min and 4-copy max"
    - "Pool cards isolated from regular deck collection"
    - "Session persists across page refresh"
  artifacts:
    - path: "e2e/sealed-mode.spec.ts"
      provides: "Automated E2E tests for sealed session flow"
      contains: "test.describe.*Sealed"
  key_links:
    - from: "Set Browser"
      to: "Sealed Session"
      via: "Start Sealed button with set code"
    - from: "Sealed Session"
      to: "Limited Deck Builder"
      via: "Build Deck button with session ID"
    - from: "e2e/sealed-mode.spec.ts"
      to: "http://localhost:9002"
      via: "Playwright baseURL"
---

<objective>
Automated E2E testing of complete sealed session flow using Playwright.
</objective>

Purpose: Replace manual human verification with automated Playwright tests. Tests catch regressions and verify the entire sealed experience works end-to-end.

Output: Automated test suite in `e2e/sealed-mode.spec.ts` covering all verification scenarios.

<execution_context>
@/home/alex/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/alex/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@e2e/deck-builder.spec.ts
@e2e/test-utils.ts
@playwright.config.ts
@.planning/phases/14-foundation/14-01-SUMMARY.md
@.planning/phases/14-foundation/14-02-SUMMARY.md
@.planning/phases/14-foundation/14-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create sealed mode E2E test file</name>
  <files>e2e/sealed-mode.spec.ts</files>
  <action>
    Create comprehensive Playwright E2E tests in `e2e/sealed-mode.spec.ts` covering all verification scenarios:

    **Test 1: Set Browser (SET-01, SET-02, SET-03)**
    - Navigate to /set-browser
    - Wait for sets to load (Scryfall API)
    - Verify sets are displayed
    - Verify sorting works (date/name)
    - Click a set and verify modal shows details
    - Click "Start Sealed" and verify navigation to /sealed

    **Test 2: Sealed Pool (SEAL-01, SEAL-02)**
    - Verify session creation
    - Verify pool shows cards (84 total expected)
    - All cards should be immediately visible

    **Test 3: Pool Filtering (SEAL-03)**
    - Click color filter buttons
    - Verify only filtered cards display
    - Test type filter
    - Test CMC filter
    - Clear filters and verify all return

    **Test 4: Limited Deck Builder (LBld-01 to LBld-06)**
    - Navigate to /limited-deck-builder or click "Build Deck"
    - Verify pool cards are available
    - Add cards to deck
    - Verify card count increases
    - Test 4-copy limit enforcement
    - Test 40-card minimum validation
    - Verify no sideboard section

    **Test 5: Pool Isolation (ISOL-01, ISOL-02, ISOL-03)**
    - Navigate to /deck-builder (regular)
    - Search for cards - pool cards should NOT appear
    - Verify limited-sessions IndexedDB store exists (separate from decks)

    **Test 6: Session Persistence**
    - Build a deck
    - Refresh page
    - Verify deck and pool persist

    Follow the existing test patterns from `e2e/deck-builder.spec.ts`:
    - Use `test.describe` for grouping
    - Use `page.goto()` for navigation
    - Use `expect()` for assertions
    - Use flexible selectors (support multiple UI patterns)
    - Include helpful test descriptions

    Note: Some tests may be skipped if specific UI elements aren't found - use `test.skip()` with explanation.
  </action>
  <verify>
    <automated>npm run test:e2e -- --grep "Sealed" 2>&1 | head -100</automated>
  </verify>
  <done>Playwright tests exist and can run against the dev server</done>
</task>

</tasks>

<verification>
- Test file created at e2e/sealed-mode.spec.ts
- Tests can be discovered and run
- At minimum: set browser navigation and basic flow tests pass
- Full suite runnable with `npm run test:e2e -- --grep "Sealed"`
</verification>

<success_criteria>
- Set browser tests: navigation, sorting, selection work
- Sealed pool tests: session creation, card display work
- Pool filtering tests: color/type/CMC filters work
- Limited deck builder tests: 40-card min, 4-copy max, no sideboard work
- Pool isolation tests: separate IndexedDB store verified
- Session persistence test: deck survives refresh
</success_criteria>

<output>
After completion, create `.planning/phases/14-foundation/14-04-SUMMARY.md`
</output>
