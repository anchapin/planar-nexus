---
phase: 14-foundation
plan: "03"
type: execute
wave: 2
depends_on:
  - "14-01"
  - "14-02"
files_modified:
  - src/lib/limited/limited-validator.ts
  - src/lib/limited/limited-validator.test.ts
  - src/app/(app)/sealed/page.tsx
  - src/app/(app)/limited-deck-builder/page.tsx
autonomous: true
requirements:
  - LBld-01
  - LBld-02
  - LBld-03
  - LBld-04
  - LBld-05
  - LBld-06

must_haves:
  truths:
    - "User can build a deck from sealed pool only"
    - "Deck builder shows 'Limited' filter restricting to pool"
    - "Deck validates 40-card minimum"
    - "Deck validates 4-copy limit per card"
    - "Deck validates no sideboard"
    - "Deck can be saved and loaded for session"
  artifacts:
    - path: "src/lib/limited/limited-validator.ts"
      provides: "40-card min, 4-copy limit, no sideboard validation"
      exports: ["validateLimitedDeck", "LIMITED_RULES"]
    - path: "src/app/(app)/sealed/page.tsx"
      provides: "Sealed session view with pool display and deck builder link"
      contains: "PoolCardGrid, session info, 'Build Deck' button"
    - path: "src/app/(app)/limited-deck-builder/page.tsx"
      provides: "Limited deck builder with pool-only card source"
      contains: "Limited filter toggle, validation errors, save deck"
  key_links:
    - from: "src/lib/limited/limited-validator.ts"
      to: "src/lib/game-rules.ts"
      via: "extends DEFAULT_RULES.limited"
      pattern: "DEFAULT_RULES\\.limited"
    - from: "src/app/(app)/limited-deck-builder/page.tsx"
      to: "src/lib/limited/pool-storage.ts"
      via: "load pool cards via session ID"
      pattern: "getSession|filterPool"
    - from: "src/app/(app)/limited-deck-builder/page.tsx"
      to: "src/lib/limited/limited-validator.ts"
      via: "validate deck on add/remove"
      pattern: "validateLimitedDeck"
---

<objective>
Create limited deck builder with pool-only card source and validation rules.
</objective>

Purpose: Allow users to build decks from their sealed pool with proper validation for limited format (40-card min, 4-copy max, no sideboard).

Output:
- Limited validator extending game-rules.ts patterns
- Sealed session page showing pool and linking to deck builder
- Limited deck builder page with pool filtering and validation

<execution_context>
@/home/alex/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/alex/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-foundation/14-RESEARCH.md
@src/lib/limited/types.ts (from Plan 01)
@src/lib/limited/pool-storage.ts (from Plan 02)
@src/lib/game-rules.ts
@src/app/(app)/deck-builder/page.tsx
@src/lib/search/filter-types.ts

<interfaces>
From src/lib/game-rules.ts:
```typescript
export const DEFAULT_RULES = {
  limited: {
    maxCopies: 4,
    minCards: 40,
    maxCards: Infinity,
    startingLife: 20,
    commanderDamage: null,
    usesSideboard: false,
    sideboardSize: 0,
  },
};

export interface ValidationResult {
  isValid: boolean;
  errors: string[];
  warnings: string[];
}

export function validateDeckFormat(
  deckCards: { name: string; count: number; color_identity?: string[]; type_line?: string }[],
  format: Format,
  commander?: { name: string; color_identity: string[] }
): FormatValidationResult;
```

From src/app/(app)/deck-builder/page.tsx:
```typescript
// Key patterns:
// - useState for deck, deckName, format
// - addCardToDeck function with maxCopies check
// - saveDeck using deckStorage.saveDeck()
// - DeckList component for display
// - CardSearch component for searching
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 0: Create limited validator test scaffold</name>
  <files>src/lib/limited/limited-validator.test.ts</files>
  <action>
    Create test scaffold for limited validator:
    
    Create `src/lib/limited/limited-validator.test.ts`:
    - Test stubs for LBld-01: pool-only deck building validation
    - Test stubs for LBld-03: 40-card minimum (should fail at 39)
    - Test stubs for LBld-04: 4-copy limit (should fail at 5 copies)
    - Test stubs for LBld-05: no sideboard (should fail if sideboard present)
    - Test stubs for LBld-06: save/load deck for session
    
    Use describe blocks matching requirement IDs. Tests should validate against LIMITED_RULES.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="limited-validator" --passWithNoTests 2>&1 | head -30</automated>
  </verify>
  <done>Test scaffold exists and can be run without errors</done>
</task>

<task type="auto">
  <name>Task 1: Create limited validator</name>
  <files>src/lib/limited/limited-validator.ts</files>
  <action>
    Create `src/lib/limited/limited-validator.ts`:
    
    1. Define LIMITED_RULES constant:
       ```typescript
       export const LIMITED_RULES = {
         maxCopies: 4,
         minCards: 40,
         maxCards: Infinity,
         startingLife: 20,
         usesSideboard: false,  // LBld-05: no sideboard
         sideboardSize: 0,
       };
       ```
    
    2. Implement `validateLimitedDeck(deck: DeckCard[]): ValidationResult`:
       - Check total cards >= 40 (LBld-03)
       - Check each card count <= 4 (LBld-04)
       - Return { isValid, errors, warnings }
    
    3. Implement `canAddCardToDeck(card: PoolCard, currentDeck: DeckCard[]): boolean`:
       - Check if adding this card would exceed 4 copies
       - Return true/false
    
    4. Implement `isPoolCard(cardId: string, pool: PoolCard[]): boolean` (LBld-01):
       - Verify card exists in pool
       - Used to prevent adding non-pool cards to deck
    
    5. Key patterns:
       - Follow validateDeckFormat pattern from game-rules.ts
       - Use ValidationResult interface
       - Include helpful error messages with actual counts
  </action>
  <verify>
    <automated>grep -l "export.*validateLimitedDeck\|export.*LIMITED_RULES" src/lib/limited/limited-validator.ts</automated>
  </verify>
  <done>Limited validator exports: validateLimitedDeck, canAddCardToDeck, isPoolCard, LIMITED_RULES</done>
</task>

<task type="auto">
  <name>Task 2: Create sealed session page</name>
  <files>src/app/(app)/sealed/page.tsx</files>
  <action>
    Create `src/app/(app)/sealed/page.tsx`:
    
    1. Read session ID from URL: `?session={id}` or `?set={code}`
    
    2. If ?set={code}: Create NEW sealed session:
       - Call generateSealedPool(setCode) from sealed-generator
       - Call createSession() from pool-storage
       - Redirect to ?session={newSessionId}
    
    3. If ?session={id}: Load existing session:
       - Call getSession(id) from pool-storage
       - Display session info: set name, card count, mode
    
    4. Display sealed pool as card grid:
       - Use PoolCard[] from session
       - Group by card name with quantity badge
       - Show filter controls: color, type, CMC
    
    5. Add filter UI (SEAL-03):
       - Color filter buttons (W, U, B, R, G)
       - Type filter dropdown (creature, instant, etc.)
       - CMC filter slider (0-7+)
       - Use filterPool() from pool-storage
    
    6. Add "Build Deck" button:
       - Navigate to `/limited-deck-builder?session={id}`
       - Pass session ID as query param
    
    7. Layout: Side-by-side or stacked card grid with filter sidebar
  </action>
  <verify>
    <automated>grep -l "'use client'" src/app/\(app\)/sealed/page.tsx && grep -l "generateSealedPool\|getSession" src/app/\(app\)/sealed/page.tsx</automated>
  </verify>
  <done>Sealed page displays pool cards with filtering, creates new sessions or loads existing</done>
</task>

<task type="auto">
  <name>Task 3: Create limited deck builder page</name>
  <files>src/app/(app)/limited-deck-builder/page.tsx</files>
  <action>
    Create `src/app/(app)/limited-deck-builder/page.tsx`:
    
    1. Read session ID from URL: `?session={id}`
    
    2. Load pool from session:
       - Call getSession(sessionId) from pool-storage
       - Extract pool cards
    
    3. Implement "Limited" mode filter (LBld-02):
       - When enabled: only show pool cards
       - Toggle button: "Collection" / "Limited"
       - In Limited mode: disable collection search, only pool cards visible
    
    4. Deck building state:
       - deck: DeckCard[] for current deck
       - Add card: Check canAddCardToDeck, update deck
       - Remove card: Decrement count, remove at 0
       - Real-time validation: Call validateLimitedDeck on each change
    
    5. Display validation status:
       - Show card count: "X / 40 cards" (LBld-03)
       - Show copy limit warnings (LBld-04)
       - No sideboard section visible (LBld-05)
    
    6. Save deck (LBld-06):
       - Call saveDeck(sessionId, deck) from pool-storage
       - Persist deck within limited session
    
    7. Layout similar to deck-builder/page.tsx:
       - Card search area (pool-only in Limited mode)
       - Deck list with validation
       - Save/Load controls
    
    8. Add "Play" button (Phase 17):
       - For now, show disabled "Play Game" button with note "Coming in Phase 17"
  </action>
  <verify>
    <automated>grep -l "'use client'" src/app/\(app\)/limited-deck-builder/page.tsx && grep -l "validateLimitedDeck\|LIMITED_RULES" src/app/\(app\)/limited-deck-builder/page.tsx</automated>
  </verify>
  <done>Limited deck builder page: pool-only filter, 40-card validation, 4-copy limit, deck saving</done>
</task>

</tasks>

<verification>
- LBld-01 through LBld-06 tests pass
- Page loads: `npm run typecheck` passes
- All validation errors display correctly
- Pool cards can be added/removed from deck
</verification>

<success_criteria>
- Limited mode filter restricts card source to pool only
- Deck shows error if below 40 cards
- Deck shows error if any card exceeds 4 copies
- No sideboard section in limited deck builder
- Deck persists within session via saveDeck()
</success_criteria>

<output>
After completion, create `.planning/phases/14-foundation/14-03-SUMMARY.md`
</output>
