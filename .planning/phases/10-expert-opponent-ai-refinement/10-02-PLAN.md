---
phase: 10-expert-opponent-ai-refinement
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ai/stack-interaction-ai.ts
  - src/ai/ai-action-executor.ts
autonomous: true
requirements:
  - REQ-13.2

must_haves:
  truths:
    - "AI can evaluate optimal targets for multi-target spells"
    - "AI handles variable cost abilities (X spells, kicker)"
    - "AI selects correct mode for modal spells"
  artifacts:
    - path: "src/ai/stack-interaction-ai.ts"
      contains: "evaluateMultiTarget"
      provides: "Multi-target evaluation method"
    - path: "src/ai/stack-interaction-ai.ts"
      contains: "evaluateVariableCost"
      provides: "Variable cost evaluation method"
    - path: "src/ai/ai-action-executor.ts"
      contains: "xValue"
      provides: "X-cost spell support"
  key_links:
    - from: "StackAction"
      to: "evaluateMultiTarget"
      via: "target evaluation"
      pattern: "evaluateMultiTarget.*target"
    - from: "executeCastSpell"
      to: "AIAction"
      via: "parameter extension"
      pattern: "xValue.*kicked.*mode"
---

<objective>
Implement support for multi-target spells and variable cost abilities in AI decision-making.

Purpose: Expert AI should handle complex card mechanics like "destroy up to X targets" or "choose one or more" effects, and evaluate variable cost abilities (X spells, kicker, modal spells).
Output: Extended StackAction type, multi-target evaluation logic, variable cost handling
</objective>

<context>
@src/ai/stack-interaction-ai.ts (StackAction interface at line 34-49 - needs extension)
@src/ai/ai-action-executor.ts (action execution - needs multi-target support)
@src/ai/stack-interaction-ai.ts (AvailableResponse type - needs variable cost fields)

Key gaps:
1. StackAction.targets only handles single target scenarios
2. No handling for modal spells (choose one)
3. No handling for X-cost abilities or kicker
4. No evaluation of optimal target selection for multi-target spells
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend StackAction and AvailableResponse types for multi-target</name>
  <files>src/ai/stack-interaction-ai.ts</files>
  <action>
Update StackAction interface (lines 34-49) to support:
1. Add 'targetCount' field: number (number of targets for multi-target spells)
2. Add 'isMultiTarget' field: boolean 
3. Add 'modalChoices' field: string[] (for "choose one" spells)
4. Add 'hasXCost' field: boolean (for X-cost spells)
5. Add 'kickerCost' field: number (additional cost for kicker)

Update AvailableResponse interface (lines 54-63):
1. Add 'targetCount' field: number (max targets we can select)
2. Add 'variableCost' field: boolean (has X or kicker)
3. Add 'modalOptions' field: string[] (if modal spell)
4. Add 'canTargetMultiple' field: boolean
</action>
  <verify>
<parameter name="automated">grep -n "isMultiTarget\|targetCount\|variableCost\|modalOptions" src/ai/stack-interaction-ai.ts | head -10</automated>
  </verify>
  <done>StackAction and AvailableResponse support multi-target and variable cost</done>
</task>

<task type="auto">
  <name>Task 2: Implement multi-target evaluation logic</name>
  <files>src/ai/stack-interaction-ai.ts</files>
  <action>
Add methods to StackInteractionAI class:

1. `evaluateMultiTargetResponse(response, context)`: 
   - Evaluates optimal target selection for multi-target spells
   - Returns array of best targets sorted by value
   - Considers: threat level, card advantage, game state impact

2. `evaluateVariableCost(response, context)`:
   - Determines optimal X value or whether to kick
   - Factors: mana available, threat level, game stage
   - Returns recommended cost to pay

3. `evaluateModalChoice(response, choice, context)`:
   - Evaluates which mode of a modal spell is best
   - Returns recommended mode

Place after the existing evaluateResponseOption method (around line 693)
</action>
  <verify>
<parameter name="automated">grep -n "evaluateMultiTarget\|evaluateVariableCost\|evaluateModalChoice" src/ai/stack-interaction-ai.ts | head -10</parameter>
  </verify>
  <done>Multi-target evaluation methods exist in StackInteractionAI</done>
</task>

<task type="auto">
<name>Task 3: Update action executor to handle complex spell targets</name>
<files>src/ai/ai-action-executor.ts</files>
<action>
Update executeCastSpell function to support:
1. Multiple targets: Accept array of targetIds, not just single
2. X-cost spells: Accept 'xValue' parameter for {X} costs
3. Kicker: Accept 'kicked' boolean parameter
4. Modal spells: Accept 'mode' parameter for "choose one" effects

Update the AIAction interface to include:
- targetIds?: string[] (array of targets)
- xValue?: number (for X-cost)
- kicked?: boolean (for kicker)
- mode?: string (for modal)

Also update getAvailableResponses to detect and mark:
- Multi-target spells (detect "up to X" or "target each" in oracle text)
- Variable cost abilities
- Modal spells
</action>
  <verify>
<parameter name="automated">grep -n "targetIds\|xValue\|kicked\|mode:" src/ai/ai-action-executor.ts | head -10</parameter>
  </verify>
  <done>Action executor supports complex spell targeting</done>
</task>

</tasks>

<verification>
- Multi-target spells have proper target evaluation
- Variable cost abilities are handled optimally
- Modal spells choose correct mode
</verification>

<success_criteria>
1. AI can evaluate and select optimal targets for multi-target spells
2. AI correctly determines X values and kicker usage
3. AI selects best mode for modal spells based on game state
</success_criteria>

<output>
After completion, create `.planning/phases/10-expert-opponent-ai-refinement/10-02-SUMMARY.md`
</output>
