---
phase: 10-expert-opponent-ai-refinement
plan: 03
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - src/ai/decision-making/combat-decision-tree.ts
  - src/ai/ai-turn-loop.ts
autonomous: true
requirements:
  - REQ-13.2

must_haves:
  truths:
    - "AI handles multi-blocker scenarios optimally"
    - "AI makes correct 2-for-1 trade decisions"
    - "Expert combat considers opponent's instant-speed removal"
  artifacts:
    - path: "src/ai/decision-making/combat-decision-tree.ts"
      contains: "evaluateTrade"
      provides: "Trade evaluation method"
    - path: "src/ai/decision-making/combat-decision-tree.ts"
      contains: "shouldMultiBlock"
      provides: "Multi-block decision method"
    - path: "src/ai/ai-turn-loop.ts"
      contains: "expert"
      provides: "Expert difficulty passed to combat"
  key_links:
    - from: "CombatDecisionTree"
      to: "DefaultCombatConfigs"
      via: "constructor"
      pattern: "DefaultCombatConfigs\\[difficulty\\]"
    - from: "runCombatPhase"
      to: "CombatDecisionTree"
      via: "instantiation"
      pattern: "new CombatDecisionTree.*difficulty"
---

<objective>
Refine combat AI for complex multi-blocker scenarios and ensure expert-level blocking decisions.

Purpose: Expert AI should handle complex blocking situations without tactical blunders - optimal blocker assignment, damage ordering, and evaluation of 2-for-1 trades.
Output: Enhanced blocking decision logic for multi-blocker scenarios
</objective>

<context>
@src/ai/decision-making/combat-decision-tree.ts (CombatDecisionTree class - already has expert config)
@src/ai/decision-making/combat-decision-tree.ts (lines 163-198: DefaultCombatConfigs - expert exists)
@src/ai/ai-turn-loop.ts (runCombatPhase - uses CombatDecisionTree)

Key gaps:
1. Need to verify multi-blocker handling exists (blocking multiple attackers)
2. Need to enhance blocker ordering logic for optimal damage distribution
3. Need to add 2-for-1 trade evaluation for expert level
4. Block decision evaluation should use expert weights from plan 10-01
</context>

<tasks>

<task type="auto">
  <name>Task 1: Review and verify existing multi-blocker handling</name>
  <files>src/ai/decision-making/combat-decision-tree.ts</files>
  <action>
Search and review existing multi-blocker code in combat-decision-tree.ts:
1. Search for "multi" or "multiple blockers" patterns
2. Review BlockDecision interface for damageOrder field (line 106)
3. Check if generateBlockPlan handles multiple blockers per attacker
4. Review blocker ordering logic (blocking order matters for trample, first strike)

Enhance if needed:
- Add explicit multi-block evaluation method if missing
- Ensure blocker ordering considers: trample, first strike, deathtouch
- Expert should make optimal order decisions

Add method `evaluateMultiBlock(blockers, attacker, gameState)` if not present
</action>
  <verify>
<parameter name="automated">grep -n "multiBlock\|evaluateBlock\|blocker.*order\|damageOrder" src/ai/decision-making/combat-decision-tree.ts | head -15</parameter>
  </verify>
  <done>Multi-blocker handling verified and enhanced</done>
</task>

<task type="auto">
  <name>Task 2: Add expert-level 2-for-1 trade evaluation</name>
  <files>src/ai/decision-making/combat-decision-tree.ts</files>
  <action>
Add advanced trade evaluation for expert difficulty:

1. Add method `evaluateTrade(blockingCreature, attackingCreature, context)`:
   - Calculate card advantage impact (trading 1-for-1, 2-for-1, etc.)
   - Consider tempo loss/gain
   - Factor in recursive value (does creature have etb/ltb triggers?)

2. Add method `shouldMultiBlock(attacker, availableBlockers, context)`:
   - Expert decision: when to block with 2+ creatures
   - Factors: trample, damage prevention, card advantage
   - Higher difficulty = more willing to 2-for-1 opponent

3. Integrate with existing evaluateBlocker method:
   - Expert: uses full evaluation including 2-for-1 potential
   - Lower difficulty: simpler evaluation

This should use expert weights from the difficulty config
</action>
  <invoke name="grep">
<parameter name="include">*.ts