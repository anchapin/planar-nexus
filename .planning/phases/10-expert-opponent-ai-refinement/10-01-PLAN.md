---
phase: 10-expert-opponent-ai-refinement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ai/stack-interaction-ai.ts
  - src/ai/game-state-evaluator.ts
  - src/ai/ai-action-executor.ts
autonomous: true
requirements:
  - REQ-13.1
  - REQ-13.2

must_haves:
  truths:
    - "Expert AI uses optimized response weights for stack decisions"
    - "AI can evaluate multiple turns ahead via lookahead"
    - "Expert difficulty stack interactions match expert difficulty config"
  artifacts:
    - path: "src/ai/stack-interaction-ai.ts"
      contains: "expert:"
      provides: "Expert response weights"
    - path: "src/ai/ai-action-executor.ts"
      contains: "evaluateLookahead"
      provides: "Lookahead evaluation function"
  key_links:
    - from: "StackInteractionAI"
      to: "DefaultResponseWeights"
      via: "constructor lookup"
      pattern: "DefaultResponseWeights\\[difficulty\\]"
    - from: "evaluateLookahead"
      to: "evaluateGameState"
      via: "recursive simulation"
      pattern: "evaluateLookahead.*evaluateGameState"
---

<objective>
Implement expert-level stack interaction decision weights and basic lookahead for complex card interactions.

Purpose: Expert AI should make optimal stack decisions (responding to player spells with protection/counters) and evaluate multiple turns ahead for complex interactions.
Output: Expert ResponseWeights, lookahead evaluation function
</objective>

<context>
@src/ai/stack-interaction-ai.ts (lines 180-232: DefaultResponseWeights - MISSING expert weights)
@src/ai/ai-difficulty.ts (lines 156-187: Expert difficulty config with lookaheadDepth=4)
@src/ai/game-state-evaluator.ts (evaluation logic used by stack AI)
@src/ai/ai-action-executor.ts (action execution)

Key gaps:
1. DefaultResponseWeights has easy/medium/hard but NOT expert
2. StackInteractionAI constructor accepts only 'easy'|'medium'|'hard'
3. lookaheadDepth is defined (expert=4) but actual lookahead code doesn't exist
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Expert ResponseWeights to StackInteractionAI</name>
  <files>src/ai/stack-interaction-ai.ts</files>
  <action>
Add expert weights to DefaultResponseWeights object (around line 232). Expert weights should be:
- threatPrevention: 2.5 (maximized - counters everything threatening)
- cardAdvantage: 2.0 (maximum - values card advantage highly)
- tempo: 1.5 (high - tempo-focused play)
- resourceConservation: 1.5 (high - efficient resource use)
- earlyGame: 0.5, midGame: 1.0, lateGame: 1.8 (adapts to game stage)
- winConditionProtection: 2.0 (maximum - protects win conditions)
- valuePlayProtection: 1.5 (high - protects value plays)
- bluffProtection: 0.8 (moderate - aware of bluffs)
- gettingCounterplayed: 1.2 (high - avoids counter traps)
- wastingRemoval: 1.5 (high - doesn't waste removal)
- fallingBehind: 1.5 (high - avoids falling behind)
- stackDepthPenalty: 0.4 (higher - understands stack complexity)
- responseEfficiency: 1.5 (high - efficient responses)
</action>
  <verify>
    <automated>grep -n "expert:" src/ai/stack-interaction-ai.ts | head -5</automated>
  </verify>
  <done>Expert weights exist in DefaultResponseWeights object</done>
</task>

<task type="auto">
  <name>Task 2: Update StackInteractionAI constructor to accept expert</name>
  <files>src/ai/stack-interaction-ai.ts</files>
  <action>
Update constructor signature at line 242-251:
1. Change difficulty parameter type from 'easy'|'medium'|'hard' to 'easy'|'medium'|'hard'|'expert'
2. Use expert weights when difficulty === 'expert'
3. Default to 'medium' if somehow undefined

Also update all convenience functions at bottom of file (lines 1277-1312):
- evaluateStackResponse
- decideCounterspell  
- manageResponseResources
All should accept 'expert' as valid difficulty
</action>
  <verify>
<automated>grep -n "difficulty: 'easy' \| 'medium' \| 'hard' \| 'expert'" src/ai/stack-interaction-ai.ts</automated>
  </verify>
  <done>StackInteractionAI accepts expert difficulty</done>
</task>

<task type="auto">
  <name>Task 3: Implement basic lookahead for complex interactions</name>
  <files>src/ai/ai-action-executor.ts, src/ai/decision-making/combat-decision-tree.ts</files>
  <action>
Create a lookahead evaluation function in ai-action-executor.ts that:

1. Simulates future game states given current state + action
2. Evaluates multiple turns ahead (depth based on difficulty)
3. Returns expected value score

Implementation approach:
- Add function `evaluateLookahead(gameState, playerId, depth): number`
- For depth=1: Evaluate current action + immediate response
- For depth=2+: Recursively simulate possible opponent responses
- Use existing evaluateGameState for each simulated state
- Expert difficulty uses depth=4 per ai-difficulty config

This function will be called by AI decision functions when useLookahead is true (expert/hard)
</action>
  <verify>
<automated>grep -n "evaluateLookahead\|lookahead" src/ai/ai-action-executor.ts | head -10</automated>
  </verify>
  <done>Lookahead function exists and is callable from AI decisions</done>
</task>

</tasks>

<verification>
- Expert difficulty stack decisions use expert weights
- AI can evaluate multiple turns ahead for complex interactions
- All existing tests pass
</verification>

<success_criteria>
1. StackInteractionAI correctly uses expert weights when difficulty='expert'
2. Lookahead evaluation function exists and returns expected value scores
3. Expert AI makes optimal stack decisions (e.g., responding to removal with protection)
</success_criteria>

<output>
After completion, create `.planning/phases/10-expert-opponent-ai-refinement/10-01-SUMMARY.md`
</output>
