---
phase: 11-search-filter-infrastructure
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(app)/deck-builder/_components/card-search.tsx
  - src/app/(app)/collection/page.tsx
  - src/lib/search/filter-cards.ts
autonomous: true
gap_closure: true
requirements:
  - SEARCH-01
  - SEARCH-02
  - SEARCH-03
  - SEARCH-04
  - SORT-01
must_haves:
  truths:
    - "Filter infrastructure integrated into UI for user access"
    - "Commander color identity rules imported from game-rules.ts"
  artifacts:
    - path: "src/app/(app)/deck-builder/_components/card-search.tsx"
      provides: "Main search UI now uses useCardFilters hook"
    - path: "src/app/(app)/collection/page.tsx"
      provides: "Collection page now uses useCardFilters hook"
    - path: "src/lib/search/filter-cards.ts"
      provides: "Commander filtering imports from game-rules.ts"
  key_links:
    - from: "src/app/(app)/deck-builder/_components/card-search.tsx"
      to: "src/hooks/use-card-filters.ts"
      via: "import useCardFilters"
    - from: "src/app/(app)/collection/page.tsx"
      to: "src/hooks/use-card-filters.ts"
      via: "import useCardFilters"
    - from: "src/lib/search/filter-cards.ts"
      to: "src/lib/game-rules.ts"
      via: "import color identity validation"
---

<objective>
Close verification gaps from Phase 11: wire filter infrastructure into UI components and eliminate code duplication.

Purpose: Connect the completed filter/sort infrastructure to the user-facing components so users can actually use these features.
Output: card-search.tsx and collection/page.tsx using useCardFilters hook
</objective>

<execution_context>
@/home/alex/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/alex/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/hooks/use-card-filters.ts (completed filter hook with filters, setFilter, sortConfig, setSort)
@src/lib/search/filter-cards.ts (completed filter functions)
@src/lib/search/filter-types.ts (completed filter types)
@src/lib/search/sort-cards.ts (completed sort functions)
@src/app/(app)/deck-builder/_components/card-search.tsx (existing search component using searchCardsOffline)
@src/app/(app)/collection/page.tsx (existing collection page using searchCardsOffline)

**Gap from VERIFICATION.md:**
- card-search.tsx: Uses searchCardsOffline directly, not useCardFilters hook
- collection/page.tsx: Uses searchCardsOffline directly, not useCardFilters hook
- filter-cards.ts: Duplicates Commander logic instead of importing from game-rules.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire useCardFilters into card-search.tsx</name>
  <files>src/app/(app)/deck-builder/_components/card-search.tsx</files>
  <action>
Replace searchCardsOffline usage with useCardFilters hook:

1. Import useCardFilters:
```typescript
import { useCardFilters } from "@/hooks/use-card-filters";
```

2. Replace local search state with hook:
```typescript
const { filters, setFilter, sortConfig, setSort, hasActiveFilters } = useCardFilters();
```

3. Replace the searchCardsOffline call with:
```typescript
// Get cards from the hook's filtered results if available, 
// otherwise fall back to searchCardsOffline for initial load
// The hook provides filteredCards when filters are applied
```

4. Keep the existing searchQuery state for fuzzy search input but wire it to setFilter('searchQuery', value)

5. Preserve the onAddCard callback and all existing UI

Note: The useCardFilters hook internally manages filtering and sorting. For card-search.tsx, you may need to:
- Load cards into the hook (via its loadCards or cards prop)
- Or use the hook's filtered/sorted results for display
- Keep backward compatibility by still supporting searchCardsOffline for initial load

The key change: replace `searchCardsOffline(searchQuery, options)` with using the filter state from useCardFilters.
  </action>
  <verify>
<automated>npm run typecheck 2>&1 | head -30</automated>
  </verify>
  <done>card-search.tsx imports and uses useCardFilters hook instead of direct searchCardsOffline call</done>
</task>

<task type="auto">
  <name>Task 2: Wire useCardFilters into collection/page.tsx</name>
  <files>src/app/(app)/collection/page.tsx</files>
  <action>
Replace searchCardsOffline usage with useCardFilters hook:

1. Import useCardFilters:
```typescript
import { useCardFilters } from "@/hooks/use-card-filters";
```

2. Initialize the hook with collection cards

3. Replace search state with hook's filter state

4. Wire search input to setFilter('searchQuery', value)

5. Wire any existing sort controls to setSort

Keep all existing collection page functionality.
  </action>
  <verify>
<automated>npm run typecheck 2>&1 | head -30</automated>
  </verify>
  <done>collection/page.tsx imports and uses useCardFilters hook instead of direct searchCardsOffline call</done>
</task>

<task type="auto">
  <name>Task 3: Refactor Commander logic to import from game-rules.ts</name>
  <files>src/lib/search/filter-cards.ts</files>
  <action>
Check if game-rules.ts has Commander color identity validation logic. If not, the existing implementation is acceptable (as it's already tested and working).

If game-rules.ts has relevant functions (check getCommanderIdentity, isCommander, or format validation):
- Import those functions
- Refactor filterByCommanderRules to use imported logic
- Keep the existing logic as fallback if imports aren't available

If game-rules.ts doesn't have specific color identity validation:
- Document the reason in a comment: "Commander logic implemented here as game-rules.ts focuses on game state, not card filtering"
- This gap is considered "won't fix" - acceptable duplication for architectural separation
  </action>
  <verify>
<automated>tsc --noEmit src/lib/search/filter-cards.ts</automated>
  </verify>
  <done>Commander filtering either imports from game-rules.ts or documents why it remains local</done>
</task>

</tasks>

<verification>
- TypeScript compilation succeeds for all modified files
- Both card-search.tsx and collection/page.tsx use useCardFilters hook
- Filter state flows through the hook to filter functions
- Sort controls use sortConfig/setSort from hook
</verification>

<success_criteria>
User can access filter features from the UI:
- Search input triggers fuzzy search via useCardFilters
- Filter controls (CMC, type, rarity, set, color, power/toughness, format) update results
- Sort dropdown changes result ordering
- Preferences persist across sessions
</success_criteria>

<output>
After completion, create `.planning/phases/11-search-filter-infrastructure/11-04-SUMMARY.md`
</output>
