---
phase: 12-saved-searches-statistics
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/search/quick-presets.ts
  - src/hooks/use-card-filters.ts
  - src/app/(app)/deck-builder/_components/card-search.tsx
autonomous: true
requirements:
  - SAVED-02
must_haves:
  truths:
    - "User can access built-in quick presets (e.g., 'Creatures under 3 mana')"
  artifacts:
    - path: "src/lib/search/quick-presets.ts"
      provides: "Built-in filter presets that users can quickly apply"
      exports: ["QUICK_PRESETS", "QuickPreset", "getPresetById"]
    - path: "src/app/(app)/deck-builder/_components/card-search.tsx"
      provides: "UI dropdown for selecting quick presets"
  key_links:
    - from: "src/hooks/use-card-filters.ts"
      to: "src/lib/search/quick-presets.ts"
      via: "Apply quick preset filters to filter state"
---

<objective>
Implement built-in quick access filter presets for common searches.

Purpose: Users can quickly apply common filter configurations without saving custom presets.
Output: Quick presets library and UI integration.
</objective>

<execution_context>
@/home/alex/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/alex/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/lib/search/filter-types.ts (FilterState, CMCFilter, TypeFilter, RarityFilter types)
@src/hooks/use-card-filters.ts (useCardFilters hook with setFilter, filters)
@src/app/(app)/deck-builder/_components/card-search.tsx (existing search component)

**FilterState structure:**
```typescript
interface FilterState {
  cmc?: CMCFilter;
  type?: TypeFilter;
  rarity?: RarityFilter;
  set?: SetFilter;
  color?: ColorFilter;
  powerToughness?: PowerToughnessFilter;
  formatLegality?: FormatLegalityFilter;
  searchQuery?: string;
}
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create quick presets library</name>
  <files>src/lib/search/quick-presets.ts</files>
  <action>
Create built-in quick presets library:

1. Define QuickPreset interface:
```typescript
import type { FilterState } from './filter-types';

export interface QuickPreset {
  id: string;
  name: string;
  description: string;
  icon?: string;
  filters: FilterState;
}
```

2. Create QUICK_PRESETS array with these presets:
- "Creatures under 3 mana": cmc: { mode: 'range', max: 2 }, type: { types: ['creature'] }
- "Powerful spells (CMC 5+)": cmc: { mode: 'range', min: 5 }
- "Legendary creatures": type: { types: ['creature'], supertypes: ['legendary'] }
- "Artifacts": type: { types: ['artifact'] }
- "Instants & Sorceries": type: { types: ['instant', 'sorcery'] }
- "Rares & Mythics": rarity: { rarities: ['rare', 'mythic'] }
- "Commons only": rarity: { rarities: ['common'] }
- "Lands": type: { types: ['land'] }
- "White cards": color: { mode: 'exact', colors: ['W'] }
- "Blue cards": color: { mode: 'exact', colors: ['U'] }
- "Black cards": color: { mode: 'exact', colors: ['B'] }
- "Red cards": color: { mode: 'exact', colors: ['R'] }
- "Green cards": color: { mode: 'exact', colors: ['G'] }
- "Colorless cards": color: { mode: 'exact', colors: [] }
- "Multicolor cards": color: { mode: 'include', colors: ['W', 'U', 'B', 'R', 'G'], min: 2 }

3. Export helper function:
- getPresetById(id: string): QuickPreset | undefined
  </action>
  <verify>
<automated>npm run typecheck 2>&1 | head -20</automated>
  </verify>
  <done>quick-presets.ts exports QUICK_PRESETS array and getPresetById function</done>
</task>

<task type="auto">
  <name>Task 2: Add quick presets UI to card-search.tsx</name>
  <files>src/app/(app)/deck-builder/_components/card-search.tsx</files>
  <action>
Add quick presets dropdown to card-search component:

1. Import QUICK_PRESETS from quick-presets.ts

2. Add a Select/Dropdown component labeled "Quick Filters" or "Quick Presets" next to the search input

3. Add handler:
- handleQuickPreset: takes a QuickPreset, clears current filters, applies the preset's filters using setFilter for each filter key

4. Add "Clear Filters" option to reset to default state

5. When filters match a quick preset, show that preset as selected in the dropdown

6. Display quick preset options grouped logically (by CMC, by type, by color, by rarity)
  </action>
  <verify>
<automated>npm run typecheck 2>&1 | head -20</automated>
  </verify>
  <done>card-search.tsx has working quick presets dropdown</done>
</task>

</tasks>

<verification>
- TypeScript compiles without errors
- Quick presets dropdown shows all built-in presets
- Selecting a quick preset applies the correct filters
- "Clear Filters" resets all filters
</verification>

<success_criteria>
User can access built-in quick presets (e.g., "Creatures under 3 mana") from a dropdown menu in the card search UI.
</success_criteria>

<output>
After completion, create `.planning/phases/12-saved-searches-statistics/12-02-SUMMARY.md`
</output>
