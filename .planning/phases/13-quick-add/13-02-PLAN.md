---
phase: 13-quick-add
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(app)/deck-builder/_components/card-search.tsx
autonomous: true
requirements:
  - QUICK-02
must_haves:
  truths:
    - "User can click a card to add it directly to deck"
    - "User can Shift+Click to add 4-of (maximum allowed)"
    - "Visual feedback appears when a card is added (flash/highlight)"
  artifacts:
    - path: "src/app/(app)/deck-builder/_components/card-search.tsx"
      provides: "Shift+Click for 4-of and visual feedback on add"
      contains: ["Shift+Click", "flash", "highlight"]
  key_links:
    - from: "card-search.tsx"
      to: "onAddCard"
      via: "onClick handler with shift detection"
---

<objective>
Implement one-click card addition with Shift+Click for 4-of and visual feedback.

Purpose: Users can quickly add cards to their deck with intuitive interactions.
Output: Enhanced click handlers with shift detection and flash animation.
</objective>

<execution_context>
@/home/alex/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/alex/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/app/(app)/deck-builder/_components/card-search.tsx (current implementation)
@src/app/(app)/deck-builder/page.tsx (addCardToDeck function)

**Current addCardToDeck (page.tsx lines 89-124):**
- Adds one copy at a time
- Checks rules.maxCopies (usually 1 for Commander, 4 for other formats)
- Shows toast when limit reached

**Shift+Click requirement:**
- If Shift key held, add up to maxCopies (usually 4) in one click
- This means calling onAddCard multiple times or modifying to accept count
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Shift+Click to add multiple copies</name>
  <files>src/app/(app)/deck-builder/_components/card-search.tsx</files>
  <action>
Implement Shift+Click functionality:

1. Modify the onClick handler on card buttons (around line 542):

```typescript
onClick={(e) => {
  // Shift+Click adds 4 copies (or max allowed)
  if (e.shiftKey) {
    const MAX_QUICK_ADD = 4;
    // Call onAddCard multiple times for quick add
    for (let i = 0; i < MAX_QUICK_ADD; i++) {
      onAddCard(card);
    }
  } else {
    onAddCard(card);
  }
}}
```

2. Update the button title and aria-label to mention Shift+Click:
```typescript
title={`Add ${card.name} to deck${hasHighSynergy ? ` (Synergy: ${Math.round(synergy.score)}%)` : ''} - Shift+Click for 4-of`}
aria-label={`Add ${card.name} to deck${hasHighSynergy ? ` (Synergy: ${Math.round(synergy.score)}%)` : ''} - Shift+Click for 4-of`}
```
  </action>
  <verify>
<automated>npm run typecheck 2>&1 | head -20</automated>
  </verify>
  <done>Shift+Click adds 4 copies to deck</done>
</task>

<task type="auto">
  <name>Task 2: Add visual flash feedback when card is added</name>
  <files>src/app/(app)/deck-builder/_components/card-search.tsx</files>
  <action>
Add visual feedback animation when a card is clicked:

1. Add state to track recently added card (for flash effect):
```typescript
const [flashCardId, setFlashCardId] = useState<string | null>(null);
```

2. Create a flash effect function:
```typescript
const triggerFlash = (cardId: string) => {
  setFlashCardId(cardId);
  setTimeout(() => setFlashCardId(null), 400);
};
```

3. Wrap the onClick to trigger flash:
```typescript
onClick={(e) => {
  triggerFlash(card.id);
  if (e.shiftKey) {
    const MAX_QUICK_ADD = 4;
    for (let i = 0; i < MAX_QUICK_ADD; i++) {
      onAddCard(card);
    }
  } else {
    onAddCard(card);
  }
}}
```

4. Add conditional class for flash animation:
```typescript
className={cn(
  'relative aspect-[5/7] w-full transform transition-transform duration-200 hover:scale-105 focus:outline-none focus:ring-2 focus:ring-ring focus:ring-offset-2 focus:ring-offset-background rounded-lg touch-manipulation group',
  flashCardId === card.id && 'animate-pulse ring-4 ring-green-500'
)}
```

5. Add the flash animation to Tailwind config (or use inline styles). If using Tailwind, ensure animate-pulse is available or add custom animation.

For the flash, use a green ring that appears briefly:
```typescript
flashCardId === card.id && 'ring-4 ring-green-500 ring-offset-2 animate-[pulse_400ms_ease-out]'
```

Or use inline style for precise timing:
```typescript
style={{
  boxShadow: flashCardId === card.id ? '0 0 20px 5px rgba(34, 197, 94, 0.8)' : undefined,
  transition: 'box-shadow 150ms ease-out'
}}
```
  </action>
  <verify>
<automated>npm run typecheck 2>&1 | head -20</automated>
  </verify>
  <done>Visual green flash appears when card is added</done>
</task>

</tasks>

<verification>
- TypeScript compiles without errors
- Regular click adds 1 card
- Shift+Click adds 4 cards
- Green flash animation appears when any card is added
- Toast notifications work (handled by page.tsx)
</verification>

<success_criteria>
User can click a card to add it directly to deck. User can Shift+Click to add 4-of. Visual feedback appears when a card is added (flash/highlight).
</success_criteria>

<output>
After completion, create `.planning/phases/13-quick-add/13-02-SUMMARY.md`
</output>
