---
phase: 13-quick-add
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(app)/deck-builder/_components/card-search.tsx
autonomous: true
requirements:
  - QUICK-01
must_haves:
  truths:
    - "User can press Enter to add highlighted card from search to deck"
    - "User can use arrow keys to navigate through search results"
    - "Selected card is visually highlighted"
  artifacts:
    - path: "src/app/(app)/deck-builder/_components/card-search.tsx"
      provides: "Keyboard navigation and Enter-to-add functionality"
      contains: ["selectedIndex", "handleKeyDown", "ArrowUp", "ArrowDown"]
  key_links:
    - from: "card-search.tsx"
      to: "onAddCard"
      via: "Enter key triggers add"
---

<objective>
Implement keyboard shortcuts for quick card addition: arrow key navigation and Enter-to-add.

Purpose: Users can navigate search results and add cards without leaving the keyboard.
Output: Enhanced card-search.tsx with keyboard navigation.
</objective>

<execution_context>
@/home/alex/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/alex/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/app/(app)/deck-builder/_components/card-search.tsx (current implementation)
@src/app/(app)/deck-builder/page.tsx (onAddCard function signature)

**Current onAddCard signature:**
```typescript
onAddCard: (card: ScryfallCard) => void;
```

**Current results rendering (line 534-574):**
- Cards are rendered in a grid: `grid-cols-2 sm:grid-cols-3 md:grid-cols-4 lg:grid-cols-3 xl:grid-cols-4`
- Each card is a `<button>` with onClick={() => onAddCard(card)}
- No current keyboard navigation
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add keyboard navigation state and handlers</name>
  <files>src/app/(app)/deck-builder/_components/card-search.tsx</files>
  <action>
Add keyboard navigation to CardSearch component:

1. Add state for selected card index:
```typescript
const [selectedIndex, setSelectedIndex] = useState<number>(-1);
```

2. Add keyboard event handler in the search input's onKeyDown:
```typescript
const handleSearchKeyDown = (e: React.KeyboardEvent) => {
  if (results.length === 0) return;
  
  switch (e.key) {
    case 'ArrowDown':
      e.preventDefault();
      setSelectedIndex(prev => 
        prev < results.length - 1 ? prev + 1 : 0
      );
      break;
    case 'ArrowUp':
      e.preventDefault();
      setSelectedIndex(prev => 
        prev > 0 ? prev - 1 : results.length - 1
      );
      break;
    case 'Enter':
      e.preventDefault();
      if (selectedIndex >= 0 && results[selectedIndex]) {
        onAddCard(results[selectedIndex]);
      }
      break;
  }
};
```

3. Add onKeyDown to the search Input component (line 316-327):
```typescript
onKeyDown={handleSearchKeyDown}
```

4. Reset selectedIndex when results change:
```typescript
useEffect(() => {
  setSelectedIndex(-1);
}, [results]);
```

5. Add visual highlight style to the selected card button:
- Add conditional class: `selectedIndex === index ? 'ring-4 ring-primary ring-offset-2' : ''`
- Ensure the button is focusable for accessibility
  </action>
  <verify>
<automated>npm run typecheck 2>&1 | head -20</automated>
  </verify>
  <done>Arrow keys navigate through results, Enter adds selected card to deck</done>
</task>

<task type="auto">
  <name>Task 2: Ensure scrollIntoView for selected card</name>
  <files>src/app/(app)/deck-builder/_components/card-search.tsx</files>
  <action>
Ensure the selected card stays visible in the scrollable area:

1. Add useRef for the results container or use the ScrollArea's viewport

2. Add useEffect to scroll selected card into view:
```typescript
useEffect(() => {
  if (selectedIndex >= 0) {
    const button = document.querySelector(`[data-card-index="${selectedIndex}"]`) as HTMLElement;
    button?.scrollIntoView({ block: 'nearest', behavior: 'smooth' });
  }
}, [selectedIndex]);
```

3. Add data-card-index to each card button:
```typescript
<button
  data-card-index={index}
  ...
>
```
  </action>
  <verify>
<automated>npm run typecheck 2>&1 | head -20</automated>
  </verify>
  <done>Selected card scrolls into view when navigating with arrow keys</done>
</task>

</tasks>

<verification>
- TypeScript compiles without errors
- Arrow keys navigate between card results
- Selected card has visual highlight (ring)
- Enter key adds highlighted card to deck
- Scrolling works properly with many results
</verification>

<success_criteria>
User can press Enter to add highlighted card from search to deck. User can use arrow keys to navigate through search results.
</success_criteria>

<output>
After completion, create `.planning/phases/13-quick-add/13-01-SUMMARY.md`
</output>
