---
phase: 9
plan: 09-03-PLAN.md
wave: 2
depends_on: [09-02-PLAN.md]
files_modified:
  - src/lib/search/orama-manager.ts
  - src/ai/tools/player-history-client.ts
must_haves:
  truths:
    - Orama provides hybrid (keyword + vector) search for history.
  artifacts:
    - Orama `game_history` index schema.
  key_links:
    - REQ-12.1: Semantic History Retrieval
---

# 09-03: Semantic History & AI Coach Tools

## Goal
Enable the AI to search and retrieve past game situations using semantic search.

<task id="1" name="Create Orama History Index">
  <files>
    <file>src/lib/search/orama-manager.ts</file>
  </files>
  <action>
    Define the `game_history` schema in Orama (id, summary, result, deck, vector) and implement `upsertGameHistory`.
  </action>
  <verify>
    <automated>
      npx jest src/lib/search/__tests__/orama-history.test.ts
    </automated>
  </verify>
  <done>
    Orama successfully indexes game summaries and supports vector search.
  </done>
</task>

<task id="2" name="History Vectorization Background Task">
  <files>
    <file>src/lib/search/background-indexing.ts</file>
  </files>
  <action>
    Add a task to `background-indexing.ts` that identifies `GameRecord`s missing vectors and generates them using `Transformers.js`.
  </action>
  <verify>
    <automated>
      # Verify vectorization progress
      npm run tsc -- --noEmit
    </automated>
  </verify>
  <done>
    Game summaries are vectorized and added to the Orama index.
  </done>
</task>

<task id="3" name="Update AI Coach Tool">
  <files>
    <file>src/ai/tools/player-history-client.ts</file>
  </files>
  <action>
    Update `playerHistoryTool` to support a `query: string` parameter that performs a hybrid search via `oramaManager.searchHistory(query)`.
  </action>
  <verify>
    <automated>
      npx jest src/ai/tools/__tests__/player-history-client-semantic.test.ts
    </automated>
  </verify>
  <done>
    AI can query historical games semantically via natural language.
  </done>
</task>
