---
phase: 9
plan: 09-02-PLAN.md
wave: 1
depends_on: [09-01-PLAN.md]
files_modified:
  - src/app/(app)/game-board/page.tsx
  - src/lib/game-history.ts
must_haves:
  truths:
    - Mistake detection requires `evaluateGameState` with `expert` weights for accuracy.
  artifacts:
    - `trackGameResult` updated to save enhanced records.
  key_links:
    - REQ-12.2: Personalized Coach Reports
---

# 09-02: Gameplay Integration & Persistence

## Goal
Collect actions and detect mistakes during gameplay using the enhanced history infrastructure.

<task id="1" name="Integrate Action Tracking in GameBoard">
  <files>
    <file>src/app/(app)/game-board/page.tsx</file>
  </files>
  <action>
    Modify `GameBoard` to maintain a local `actions: GameAction[]` array and append each engine action as it occurs.
  </action>
  <verify>
    <automated>
      # Manual verification by playing a game is needed, but unit tests for tracker logic are possible.
      npx jest src/app/(app)/game-board/__tests__/action-tracking.test.tsx
    </automated>
  </verify>
  <done>
    `GameBoard` successfully tracks all player actions in a local state.
  </done>
</task>

<task id="2" name="Implement Mistake Detection Logic">
  <files>
    <file>src/app/(app)/game-board/page.tsx</file>
  </files>
  <action>
    On each player action, call `evaluateGameState` before and after. If the score delta is significant (> 2.0), add a string describing the mistake to a `mistakes: string[]` array.
  </action>
  <verify>
    <automated>
      npm run tsc -- --noEmit
    </automated>
  </verify>
  <done>
    Significant score drops are detected and stored as mistake descriptions.
  </done>
</task>

<task id="3" name="Persistence Update">
  <files>
    <file>src/app/(app)/game-board/page.tsx</file>
  </files>
  <action>
    Update `trackGameResult` to generate a summary using `summarizeGame` and save the extended `GameRecord` (with `actions`, `mistakes`, and `summary`) to IndexedDB using `saveGameRecord`.
  </action>
  <verify>
    <automated>
      # Check persistence in IndexedDB after a test game
      npx playwright test e2e/game-history-persistence.spec.ts
    </automated>
  </verify>
  <done>
    Enhanced game records are successfully persisted in IndexedDB on game end.
  </done>
</task>
