---
phase: 09-adaptive-coaching
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/game-history.ts
  - src/app/(app)/layout.tsx
  - src/components/indexeddb-migration.tsx
autonomous: true
gap_closure: true
requirements:
  - REQ-12.1

must_haves:
  truths:
    - "Game history reads from IndexedDB instead of localStorage"
    - "New game records are saved to IndexedDB"
    - "Historical data is migrated on first app load"
  artifacts:
    - path: "src/lib/game-history.ts"
      provides: "Game record read/write using IndexedDB"
    - path: "src/components/indexeddb-migration.tsx"
      provides: "Migration trigger on app mount"
  key_links:
    - from: "src/lib/game-history.ts"
      to: "src/lib/indexeddb-storage.ts"
      via: "indexedDBStorage.getAll() / set()"
      pattern: "indexedDBStorage\\.(getAll|set)\\("
    - from: "src/components/indexeddb-migration.tsx"
      to: "src/lib/indexeddb-storage.ts"
      via: "migrateGameHistoryToIndexedDB()"
      pattern: "migrateGameHistoryToIndexedDB"
---

<objective>
Integrate IndexedDB storage into game history module and ensure migration runs on app initialization. This closes gaps where:
1. `getAllGameRecords()` and `saveGameRecord()` still use localStorage instead of IndexedDB
2. The migration function `migrateGameHistoryToIndexedDB()` is never called during app initialization
</objective>

<context>
@src/lib/game-history.ts
@src/lib/indexeddb-storage.ts (lines 644-660 for migration function, lines 299-315 for getAll API)
@src/app/(app)/layout.tsx

**IndexedDB Storage API:**
- `indexedDBStorage.getAll<T>(storeName: string): Promise<T[]>` - Get all items
- `indexedDBStorage.set<T>(storeName: string, value: T & { id: string }): Promise<void>` - Set single item (requires `id` property)
- `migrateGameHistoryToIndexedDB()` - Migrates from localStorage to IndexedDB 'game-history' store
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update game-history.ts to use IndexedDB</name>
  <files>src/lib/game-history.ts</files>
  <action>
    1. Import `indexedDBStorage` from `src/lib/indexeddb-storage.ts`
    2. Update `getAllGameRecords()` to read from IndexedDB 'game-history' store:
       - Call `await indexedDBStorage.getAll<GameRecord>('game-history')`
       - Fall back to localStorage if IndexedDB returns empty array (backward compatibility)
    3. Update `saveGameRecord()` to write to IndexedDB:
       - Call `await indexedDBStorage.set('game-history', record)` 
       - Also write to localStorage for backward compatibility during transition
       - Keep the 1000 game limit logic
    4. Update `clearGameHistory()` to clear both IndexedDB and localStorage
    5. Update `getPlayerStats()` - no changes needed as it uses `getAllGameRecords()`
    6. Update `getRecentGames()` - no changes needed as it uses `getAllGameRecords()`
  </action>
  <verify>
    <automated>TypeScript compiles: npx tsc --noEmit --skipLibCheck 2>&1 | head -20</automated>
  </verify>
  <done>getAllGameRecords() reads from IndexedDB, saveGameRecord() writes to both IndexedDB and localStorage, clearGameHistory() clears both</done>
</task>

<task type="auto">
  <name>Task 2: Create IndexedDB migration component</name>
  <files>src/components/indexeddb-migration.tsx</files>
  <action>
    Create a client component that runs migration on app mount:
    ```tsx
    'use client';
    
    import { useEffect, useState } from 'react';
    import { migrateGameHistoryToIndexedDB } from '@/lib/indexeddb-storage';
    
    export function IndexedDBMigration() {
      const [migrated, setMigrated] = useState(false);
      
      useEffect(() => {
        if (!migrated) {
          migrateGameHistoryToIndexedDB()
            .then(() => setMigrated(true))
            .catch(console.error);
        }
      }, [migrated]);
      
      // This component doesn't render anything
      return null;
    }
    ```
  </action>
  <verify>
    <automated>File exists: ls src/components/indexeddb-migration.tsx</automated>
  </verify>
  <done>Migration component created, runs on first app load</done>
</task>

<task type="auto">
  <name>Task 3: Integrate migration into app layout</name>
  <files>src/app/(app)/layout.tsx</files>
  <action>
    1. Import the migration component: `import { IndexedDBMigration } from '@/components/indexeddb-migration';`
    2. Add `<IndexedDBMigration />` inside the `<SidebarProvider>` component (before children)
    - This ensures migration runs for all authenticated app routes
  </action>
  <verify>
    <automated>TypeScript compiles: npx tsc --noEmit --skipLibCheck 2>&1 | head -20</automated>
  </verify>
  <done>Migration runs when any app route is loaded</done>
</task>

</tasks>

<verification>
After execution:
- New game records saved via saveGameRecord() are persisted to IndexedDB 'game-history' store
- getAllGameRecords() reads from IndexedDB (with localStorage fallback)
- Migration runs on first app load, populating IndexedDB from localStorage
- Semantic search can access game history from IndexedDB
</verification>

<success_criteria>
- Gap 1 closed: getAllGameRecords() and saveGameRecord() now use IndexedDB
- Gap 2 resolved: Game history data flows into IndexedDB for semantic search
- REQ-12.1 (Semantic History Retrieval) requirements addressed
</success_criteria>

<output>
After completion, create `.planning/phases/09-adaptive-coaching/09-05-SUMMARY.md`
</output>
