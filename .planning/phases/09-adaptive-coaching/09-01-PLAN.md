---
phase: 9
plan: 09-01-PLAN.md
wave: 1
depends_on: []
files_modified:
  - src/lib/game-history.ts
  - src/lib/indexeddb-storage.ts
  - src/lib/game-summarizer.ts
must_haves:
  truths:
    - Game history must be migrated from localStorage to IndexedDB to avoid quota issues.
  artifacts:
    - src/lib/game-summarizer.ts: A new utility to convert actions into text.
  key_links:
    - REQ-12.1: Semantic History Retrieval
---

# 09-01: Game History Infrastructure & Migration

## Goal
Upgrade the game history infrastructure to handle larger datasets (actions, mistakes) and migrate to IndexedDB.

<task id="1" name="Update GameRecord Types">
  <files>
    <file>src/lib/game-history.ts</file>
  </files>
  <action>
    Update the `GameRecord` interface to include `actions: GameAction[]`, `mistakes: string[]`, and `summary: string`.
  </action>
  <verify>
    <automated>
      npm run tsc -- --noEmit
    </automated>
  </verify>
  <done>
    `GameRecord` interface is updated and code compiles.
  </done>
  <status>completed</status>
</task>

<task id="2" name="Configure IndexedDB for Game History">
  <files>
    <file>src/lib/indexeddb-storage.ts</file>
  </files>
  <action>
    Add `game-history` to the `StorageConfig` stores and implement `migrateGameHistoryToIndexedDB` to move records from `localStorage` ('planar-nexus-game-history').
  </action>
  <verify>
    <automated>
      # Verify migration logic with a new test
      npx jest src/lib/__tests__/indexeddb-migration-game-history.test.ts
    </automated>
  </verify>
  <done>
    IndexedDB has a `game-history` store and migration logic is implemented.
  </done>
  <status>completed</status>
</task>

<task id="3" name="Implement Game Summarizer">
  <files>
    <file>src/lib/game-summarizer.ts</file>
  </files>
  <action>
    Create a new utility `summarizeGame(actions: GameAction[], result: GameResult, playerDeck: string): string` that produces a concise natural language summary of the game.
  </action>
  <verify>
    <automated>
      npx jest src/lib/__tests__/game-summarizer.test.ts
    </automated>
  </verify>
  <done>
    `src/lib/game-summarizer.ts` is created and passes tests.
  </done>
  <status>completed</status>
</task>
