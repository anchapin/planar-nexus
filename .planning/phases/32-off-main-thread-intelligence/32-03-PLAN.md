---
phase: 32-off-main-thread-intelligence
plan: 03
type: execute
wave: 3
depends_on: ["32-01", "32-02"]
files_modified:
  - src/ai/worker/worker-types.ts
  - src/ai/worker/ai-worker.ts
  - src/ai/worker/__tests__/context-digestion.test.ts
  - src/ai/flows/context-builder.ts
  - src/ai/flows/__tests__/context-optimization.test.ts
  - src/ai/flows/genkit-coach-flow.ts
  - src/app/api/chat/coach/route.ts
  - src/hooks/use-deck-coach-chat.ts
  - scripts/benchmark-ai-latency.ts
autonomous: true
requirements: [WORKER-03]

must_haves:
  truths:
    - "Genkit coach calls use significantly smaller payloads for large decks"
    - "Board context is intelligently included in AI coach conversations when in a game"
    - "AI Worker pre-processes context in the background before LLM calls"
  artifacts:
    - path: "src/ai/worker/ai-worker.ts"
      provides: "Context digestion logic"
    - path: "src/ai/flows/context-builder.ts"
      provides: "Compact context formatting for LLM"
    - path: "src/hooks/use-deck-coach-chat.ts"
      provides: "Optimized API call with digested context"
  key_links:
    - from: "src/hooks/use-deck-coach-chat.ts"
      to: "src/ai/worker/ai-worker-client.ts"
      via: "context digestion request"
---

<objective>
Optimize Genkit flows by implementing selective context pre-fetching and payload reduction using the AI Worker.

Purpose: Reduce latency and token usage for AI coach conversations, especially for large decks and active game states.
Output: An optimized context-preparation pipeline that digests deck and board data into a compact, LLM-friendly format.
</objective>

<execution_context>
@/home/alex/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-off-main-thread-intelligence/32-01-PLAN.md
@.planning/phases/32-off-main-thread-intelligence/32-02-PLAN.md
@src/ai/flows/context-builder.ts
@src/ai/flows/genkit-coach-flow.ts
@src/hooks/use-deck-coach-chat.ts
@src/app/api/chat/coach/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Context Digestion in AI Worker</name>
  <files>src/ai/worker/worker-types.ts, src/ai/worker/ai-worker.ts, src/ai/worker/__tests__/context-digestion.test.ts</files>
  <action>
    Enhance the AI Worker to support background context processing.
    - Update `AIWorkerAction` in `src/ai/worker/worker-types.ts` to include `PREPARE_COACH_CONTEXT`.
    - Implement a `DigestManager` in `src/ai/worker/ai-worker.ts` that takes a full deck and/or game state.
    - Create a "Compact Context" format.
    - Create `src/ai/worker/__tests__/context-digestion.test.ts` to verify the digestion logic.
  </action>
  <verify>
    <automated>npx vitest run src/ai/worker/__tests__/context-digestion.test.ts</automated>
  </verify>
  <done>AI Worker can generate compact context summaries for LLM consumption.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor Context Builder and Genkit Flow</name>
  <files>src/ai/flows/context-builder.ts, src/ai/flows/genkit-coach-flow.ts, src/app/api/chat/coach/route.ts, src/ai/flows/__tests__/context-optimization.test.ts</files>
  <action>
    Optimize the Genkit flow to handle pre-digested context.
    - Add `formatDigestedContextForLLM` to `src/ai/flows/context-builder.ts`.
    - Update `genkit-coach-flow.ts` to accept an optional `digestedContext` object in its input schema.
    - Create `src/ai/flows/__tests__/context-optimization.test.ts` to verify the optimized flow input.
  </action>
  <verify>
    <automated>npx vitest run src/ai/flows/__tests__/context-optimization.test.ts</automated>
  </verify>
  <done>Genkit flow and API route support optimized, reduced payloads.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate and Benchmark Latency Optimization</name>
  <files>src/hooks/use-deck-coach-chat.ts, scripts/benchmark-ai-latency.ts</files>
  <action>
    Integrate the optimized context into the chat hook and create a benchmark script.
    - Refactor `useDeckCoachChat` to use `digestedContext`.
    - Create `scripts/benchmark-ai-latency.ts` to measure the 25% latency reduction target.
  </action>
  <verify>
    <automated>npx ts-node scripts/benchmark-ai-latency.ts</automated>
  </verify>
  <done>Latency optimization verified: 25% reduction achieved.</done>
</task>

</tasks>

<verification>
- Monitor network requests to `/api/chat/coach` for large decks (100+ cards).
- Verify payload size reduction (target: >50% reduction for Commander decks).
- Confirm the AI coach correctly references current board state (e.g., "You have 3 mana available and a threat on board").
- Ensure UI responsiveness remains at 60fps during context digestion.
</verification>

<success_criteria>
- AI Coach payloads are reduced for large decks.
- Board state is contextually available to the coach via the worker digest.
- No regression in AI coach strategic advice quality.
</success_criteria>

<output>
After completion, create `.planning/phases/32-off-main-thread-intelligence/32-03-SUMMARY.md`
</output>
