---
phase: 32-off-main-thread-intelligence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ai/worker/worker-types.ts
  - src/ai/worker/ai-worker.ts
  - src/ai/worker/ai-worker-client.ts
  - src/ai/worker/__tests__/ai-worker.test.ts
  - src/ai/worker/__tests__/ai-worker-client.test.ts
  - package.json
autonomous: true
requirements: [WORKER-01, WORKER-02]
must_haves:
  truths:
    - "Heuristic AI engine runs in a dedicated Web Worker"
    - "AI Worker client can communicate with the background thread"
  artifacts:
    - path: "src/ai/worker/ai-worker.ts"
      provides: "Web Worker entry point"
      exports: ["analyzeGameState", "evaluateBoard", "quickScore"]
    - path: "src/ai/worker/worker-types.ts"
      provides: "Type definitions for worker messages and payloads"
    - path: "src/ai/worker/ai-worker-client.ts"
      provides: "Main thread bridge and proxy for the AI Worker"
  key_links:
    - from: "src/ai/worker/ai-worker-client.ts"
      to: "src/ai/worker/ai-worker.ts"
      via: "Web Worker postMessage or Comlink"
---

<objective>
Create the AI Web Worker infrastructure to offload heuristic gameplay calculations from the main thread.

Purpose: Maintain UI responsiveness (60fps) by preventing long-running AI calculations from blocking the event loop.
Output: A dedicated Web Worker for AI analysis and a client-side bridge for communication.
</objective>

<execution_context>
@/home/alex/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-off-main-thread-intelligence/RESEARCH.md
@src/ai/game-state-evaluator.ts
@src/ai/flows/ai-gameplay-assistance.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Dependencies and Define AI Worker Contracts</name>
  <files>package.json, src/ai/worker/worker-types.ts</files>
  <action>
    - Install `comlink` (production dependency).
    - Install `vitest-web-worker` and `ts-node` (dev dependencies).
    - Create `src/ai/worker/worker-types.ts` to define the communication protocol between the main thread and the AI Worker.
    - Define `AIWorkerAction` enum (e.g., ANALYZE_STATE, EVALUATE_BOARD, QUICK_SCORE, DETECT_ARCHETYPE).
    - Define payload types for each action (e.g., `AnalyzeStatePayload` with `GameState` and `PlayerId`).
    - Define response types including results and error states.
    - Ensure all types are transferable-friendly (no circular references).
  </action>
  <verify>
    <automated>grep '"comlink":' package.json && npx tsc src/ai/worker/worker-types.ts --noEmit</automated>
  </verify>
  <done>Dependencies are installed and AI Worker contracts are defined.</done>
</task>

<task type="auto">
  <name>Task 2: Implement AI Worker Integration Test Suite</name>
  <files>src/ai/worker/__tests__/ai-worker.test.ts</files>
  <action>
    Create a robust test suite in `src/ai/worker/__tests__/ai-worker.test.ts` to verify worker behavior.
    - Mock the Web Worker environment using `vitest-web-worker`.
    - Test serialization/deserialization of `GameState` to ensure no circular references cause crashes.
    - Test error handling within the worker context.
  </action>
  <verify>
    <automated>ls src/ai/worker/__tests__/ai-worker.test.ts</automated>
  </verify>
  <done>Integration test suite for the AI Worker is ready.</done>
</task>

<task type="auto">
  <name>Task 3: Implement AI Worker</name>
  <files>src/ai/worker/ai-worker.ts</files>
  <action>
    Create `src/ai/worker/ai-worker.ts` as the Web Worker entry point.
    - Import `evaluateGameState`, `quickScore` from `src/ai/game-state-evaluator.ts`.
    - Implement a message listener handling incoming AI analysis requests using Comlink `expose`.
    - Wrap calculations in try/catch blocks and send error messages back to the main thread if needed.
  </action>
  <verify>
    <automated>npx vitest run src/ai/worker/__tests__/ai-worker.test.ts</automated>
  </verify>
  <done>AI Worker is implemented and passes integration tests.</done>
</task>

<task type="auto">
  <name>Task 4: Create AI Worker Client Bridge</name>
  <files>src/ai/worker/ai-worker-client.ts, src/ai/worker/__tests__/ai-worker-client.test.ts</files>
  <action>
    Create `src/ai/worker/ai-worker-client.ts`.
    - Implement a singleton `AIWorkerClient` class that initializes the Web Worker.
    - Use `Comlink.wrap` to create a proxy for the worker's API.
    - Provide a clean interface for components to call AI analysis methods.
    - Create `src/ai/worker/__tests__/ai-worker-client.test.ts` to verify RPC communication and error handling.
  </action>
  <verify>
    <automated>npx vitest run src/ai/worker/__tests__/ai-worker-client.test.ts</automated>
  </verify>
  <done>Main-thread bridge for the AI Worker is ready and verified.</done>
</task>

</tasks>

<verification>
- AI Worker initializes without errors.
- Basic `quickScore` calculation can be offloaded to the worker and returns correct results.
- No "Blocked main thread" warnings in console during AI analysis.
</verification>

<success_criteria>
- AI Worker is active and responding to requests.
- Heuristic logic can be invoked via `AIWorkerClient`.
</success_criteria>

<output>
After completion, create `.planning/phases/32-off-main-thread-intelligence/32-01-SUMMARY.md`
</output>
