---
phase: 32-off-main-thread-intelligence
plan: 02
type: execute
wave: 2
depends_on: ["32-01"]
files_modified:
  - src/hooks/use-ai-worker.ts
  - src/hooks/__tests__/use-ai-worker.test.ts
  - src/components/ai/AIThinkingIndicator.tsx
  - src/components/ai/__tests__/AIThinkingIndicator.test.ts
  - src/app/(app)/game-board/page.tsx
  - e2e/ai-worker.spec.ts
  - e2e/performance/ai-performance.spec.ts
autonomous: true
requirements: [WORKER-02, WORKER-04]
must_haves:
  truths:
    - "AI 'thinking' state is reactively available in components"
    - "UI displays non-blocking indicator during background AI analysis"
  artifacts:
    - path: "src/hooks/use-ai-worker.ts"
      provides: "Reactive interface to the AI worker"
      exports: ["useAIWorker"]
    - path: "src/components/ai/AIThinkingIndicator.tsx"
      provides: "Visual feedback for AI background tasks"
  key_links:
    - from: "src/hooks/use-ai-worker.ts"
      to: "src/ai/worker/ai-worker-client.ts"
      via: "Direct proxy calls"
---

<objective>
Connect the AI Worker to React components using a custom hook and implement visual "thinking" indicators.

Purpose: Provide user feedback when background calculations are in progress and maintain 60fps UI performance.
Output: A reusable `useAIWorker` hook and a non-blocking AI status indicator component.
</objective>

<execution_context>
@/home/alex/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-off-main-thread-intelligence/32-01-PLAN.md
@src/ai/worker/ai-worker-client.ts
@src/app/(app)/game-board/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create useAIWorker Hook</name>
  <files>src/hooks/use-ai-worker.ts, src/hooks/__tests__/use-ai-worker.test.ts</files>
  <action>
    Create a custom hook `src/hooks/use-ai-worker.ts` to manage interaction with the `AIWorkerClient`.
    - Maintain internal state for `isThinking`, `lastResult`, and `error`.
    - Provide functions like `analyzeState(gameState, playerId)` that call the worker client.
    - Implement a request-cancellation mechanism if a new analysis is triggered before the old one finishes.
    - Create `src/hooks/__tests__/use-ai-worker.test.ts` to test hook behavior and result handling.
  </action>
  <verify>
    <automated>npx vitest run src/hooks/__tests__/use-ai-worker.test.ts</automated>
  </verify>
  <done>Custom hook for reactive AI Worker integration is available and tested.</done>
</task>

<task type="auto">
  <name>Task 2: Implement AI Thinking Indicator</name>
  <files>src/components/ai/AIThinkingIndicator.tsx, src/components/ai/__tests__/AIThinkingIndicator.test.ts</files>
  <action>
    Create a `src/components/ai/AIThinkingIndicator.tsx` component to provide non-blocking visual feedback.
    - Implement a pulse animation or discrete progress indicator.
    - Design it to be unobtrusive (integrated into the AI Coach avatar).
    - Create `src/components/ai/__tests__/AIThinkingIndicator.test.ts` to verify component rendering and animation state.
  </action>
  <verify>
    <automated>npx vitest run src/components/ai/__tests__/AIThinkingIndicator.test.ts</automated>
  </verify>
  <done>AI status indicator component is implemented and verified.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate AI Worker into Game Board</name>
  <files>src/app/(app)/game-board/page.tsx, e2e/ai-worker.spec.ts</files>
  <action>
    Refactor `src/app/(app)/game-board/page.tsx` to use the `useAIWorker` hook.
    - Replace synchronous `evaluateGameState` calls with the asynchronous worker call.
    - Conditionally render the `AIThinkingIndicator` when `isThinking` is true.
    - Create `e2e/ai-worker.spec.ts` to verify the end-to-end integration.
  </action>
  <verify>
    <automated>npx playwright test e2e/ai-worker.spec.ts</automated>
  </verify>
  <done>Game Board UI is fully integrated with the off-main-thread AI engine.</done>
</task>

<task type="auto">
  <name>Task 4: Performance Validation (60fps Gate)</name>
  <files>e2e/performance/ai-performance.spec.ts</files>
  <action>
    Create an E2E performance test to ensure AI analysis does not cause frame drops.
    - Use Playwright's CDP session to track long tasks.
    - Assert that no task exceeds 16ms during active AI thinking.
    - Verify that UI remains responsive to hover and click events.
  </action>
  <verify>
    <automated>npx playwright test e2e/performance/ai-performance.spec.ts</automated>
  </verify>
  <done>Performance gate is verified: UI remains responsive (60fps) during AI analysis.</done>
</task>

</tasks>

<verification>
- AI analysis is triggered without causing any frame drops.
- "Thinking" indicator appears and disappears correctly during AI analysis.
- UI remains responsive to user clicks and hover states during background processing.
</verification>

<success_criteria>
- AI Coach analysis is offloaded to the worker.
- Status indicators correctly reflect the background activity.
- User can interact with the board while AI is thinking.
</success_criteria>

<output>
After completion, create `.planning/phases/32-off-main-thread-intelligence/32-02-SUMMARY.md`
</output>
