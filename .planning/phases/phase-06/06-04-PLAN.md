---
phase: phase-06
plan: 04
type: execute
wave: 4
depends_on: [06-03-PLAN.md]
files_modified: [src/hooks/use-game-chat.ts, src/components/ai-coach/chat-panel.tsx]
autonomous: true
requirements: [REQ-9.2, REQ-9.3]
must_haves:
  truths:
    - Frontend useChat hook correctly points to the new /api/chat endpoint.
    - Client-side player history tool is integrated and functional.
  artifacts:
    - path: src/hooks/use-game-chat.ts
      provides: Unified hook for game chat with tool support.
      min_lines: 40
    - path: src/components/ai-coach/chat-panel.tsx
      provides: Streaming chat UI for the AI coach.
      min_lines: 60
  key_links:
    - from: src/hooks/use-game-chat.ts
      to: src/app/api/chat/route.ts
      via: useChat API endpoint
    - from: src/hooks/use-game-chat.ts
      to: src/ai/tools/player-history-client.ts
      via: tool injection
---

<objective>
Integrate the frontend `useChat` hook with the new streaming backend and client-side tools.

Purpose: Provide a seamless, real-time AI conversation experience with incremental updates and access to local player history.
Output: Refactored game chat and AI coach panels using Vercel AI SDK frontend hooks.
</objective>

<execution_context>
@/home/alex/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@src/hooks/use-game-chat.ts
@src/app/api/chat/route.ts
@src/components/ai-coach/chat-panel.tsx
@src/ai/tools/player-history-client.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Game Chat Hook with Tools</name>
  <files>src/hooks/use-game-chat.ts</files>
  <action>
    Refactor `use-game-chat.ts` to use `useChat` from the Vercel AI SDK:
    - Update the `api` endpoint to `/api/chat`.
    - Add `getPlayerHistory` to the `tools` property of `useChat`.
    - Set `maxSteps` to allow the LLM to call tools and get results in a single interaction.
    - Ensure game-specific context is still passed appropriately.
  </action>
  <verify>
    <automated>ls src/hooks/use-game-chat.ts</automated>
  </verify>
  <done>Game chat hook successfully migrated with client-side tool support.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor AI Coach UI for Streaming</name>
  <files>src/components/ai-coach/chat-panel.tsx</files>
  <action>
    Update the chat panel component to handle streaming responses:
    - Ensure the UI updates incrementally as tokens are received.
    - Use the state provided by `use-game-chat.ts`.
    - Display status messages when tools are being executed.
  </action>
  <verify>
    <automated>ls src/components/ai-coach/chat-panel.tsx</automated>
  </verify>
  <done>AI Coach UI now supports real-time streaming updates and tool execution status.</done>
</task>

</tasks>

<verification>
Interact with the AI Coach in the game. Ask for your history to verify the client-side tool is called.
</verification>

<success_criteria>
1. Real-time streaming is visible in the chat UI.
2. User input remains responsive during streaming.
3. LLM can successfully query player history from localStorage via the client tool.
</success_criteria>

<output>
After completion, create `.planning/phases/phase-06/phase-06-04-SUMMARY.md`
</output>
