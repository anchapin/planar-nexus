---
phase: phase-06
plan: 03
type: execute
wave: 3
depends_on: [06-02-PLAN.md]
files_modified: [src/app/api/chat/route.ts, src/ai/tools/card-search.ts, src/ai/tools/player-history-client.ts]
autonomous: true
requirements: [REQ-9.3]
must_haves:
  truths:
    - Server-side MCP tool for card search is correctly implemented and registered.
    - Client-side tool definition for player history is prepared for useChat.
  artifacts:
    - path: src/ai/tools/card-search.ts
      provides: Server-side tool for searching the card database.
      min_lines: 20
    - path: src/ai/tools/player-history-client.ts
      provides: Client-side tool definition for player history retrieval.
      min_lines: 15
  key_links:
    - from: src/app/api/chat/route.ts
      to: src/ai/tools/card-search.ts
      via: tool registration
---

<objective>
Implement Model Context Protocol (MCP) bridge for local card database and player history access.

Purpose: Allow the LLM to search and reason about card data and player history using standardized tools.
Output: Integrated `tools` in the `/api/chat` route (server) and prepared tools for `useChat` (client).
</objective>

<execution_context>
@/home/alex/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/phase-06/RESEARCH.md
@src/lib/server-card-operations.ts
@src/app/api/chat/route.ts
@src/lib/game-history.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Server-Side Card Search Tool</name>
  <files>src/ai/tools/card-search.ts</files>
  <action>
    Create MCP-compliant tool for card database access on the server:
    - Implement `searchCards` tool using Vercel AI SDK `tool` utility.
    - Parameterize it with a search query (string) and optional filters (Zod schema).
    - Map the `execute` function to `src/lib/server-card-operations.ts`.
    - Provide clear tool descriptions.
  </action>
  <verify>
    <automated>npx vitest src/ai/tools/card-search.test.ts</automated>
  </verify>
  <done>Card search tool defined and verified.</done>
</task>

<task type="auto">
  <name>Task 2: Define Client-Side Player History Tool</name>
  <files>src/ai/tools/player-history-client.ts</files>
  <action>
    Create a tool definition for `getPlayerHistory` that will run on the client:
    - Define the Zod schema for input (e.g., limit).
    - Implement the `execute` function to read from `localStorage` via `src/lib/game-history.ts`.
    - This tool will be passed to the `useChat` hook in the next wave.
  </action>
  <verify>
    <automated>ls src/ai/tools/player-history-client.ts</automated>
  </verify>
  <done>Player history client tool defined.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate Server Tools into Chat API</name>
  <files>src/app/api/chat/route.ts</files>
  <action>
    Register the `searchCards` tool in the server-side `streamText` call:
    - Add it to the `tools` object.
    - Ensure the LLM is informed about the tool's availability.
  </action>
  <verify>
    <automated>ls src/app/api/chat/route.ts</automated>
  </verify>
  <done>Server tools integrated into the unified chat endpoint.</done>
</task>

</tasks>

<verification>
Unit test the `searchCards` tool with Vitest. Verify `player-history-client.ts` exports a valid tool object.
</verification>

<success_criteria>
1. `searchCards` tool correctly queries the database.
2. `getPlayerHistory` tool is correctly defined for client-side use.
3. Server-side chat route correctly exposes the card search tool to the LLM.
</success_criteria>

<output>
After completion, create `.planning/phases/phase-06/phase-06-03-SUMMARY.md`
</output>
