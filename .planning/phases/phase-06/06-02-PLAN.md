---
phase: phase-06
plan: 02
type: execute
wave: 2
depends_on: [06-01-PLAN.md]
files_modified: [src/app/api/ai-proxy/route.ts, src/ai/providers/factory.ts]
autonomous: true
requirements: [REQ-9.1]
must_haves:
  truths:
    - AI proxy endpoint supports streaming responses via Vercel AI SDK.
    - Legacy features remain functional after proxy refactor.
  artifacts:
    - path: src/app/api/ai-proxy/route.ts
      provides: Refactored AI proxy delegating to Vercel AI SDK.
      min_lines: 50
  key_links:
    - from: src/app/api/ai-proxy/route.ts
      to: src/ai/providers/factory.ts
      via: getAIModel import
---

<objective>
Migrate the existing AI proxy logic to the unified Vercel AI SDK backend.

Purpose: Deprecate the manual proxy logic and ensure all existing AI features use the new streaming infrastructure.
Output: Refactored `ai-proxy` that delegates to the Vercel AI SDK factory.
</objective>

<execution_context>
@/home/alex/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/phase-06/06-01-PLAN.md
@src/app/api/ai-proxy/route.ts
@src/app/api/chat/route.ts
@src/ai/providers/factory.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Factory for Legacy Compatibility</name>
  <files>src/ai/providers/factory.ts</files>
  <action>
    Ensure `getAIModel` supports the legacy provider names and model IDs used in the current `ai-proxy`:
    - Map `zaic` (if still needed) or provide a clear fallback.
    - Ensure Anthropic and Google Gemini models are correctly mapped from legacy identifiers.
  </action>
  <verify>
    <automated>npx vitest src/ai/providers/factory.test.ts</automated>
  </verify>
  <done>Factory supports all legacy and new provider/model combinations.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor AI Proxy to Delegate to Chat Route</name>
  <files>src/app/api/ai-proxy/route.ts</files>
  <action>
    Refactor `src/app/api/ai-proxy/route.ts` to use the new `streamText` logic:
    - Replace the manual `ReadableStream` and `TextEncoder` logic with Vercel AI SDK.
    - Maintain support for existing request/response formats to avoid breaking current frontend features.
    - Standardize on `createDataStreamResponse`.
  </action>
  <verify>
    <automated>ls src/app/api/ai-proxy/route.ts</automated>
  </verify>
  <done>Legacy proxy refactored to use the new SDK infrastructure while maintaining backward compatibility.</done>
</task>

</tasks>

<verification>
Test existing AI features (e.g., AI Coach) to ensure they still function and now use streaming chunks correctly.
</verification>

<success_criteria>
1. `POST /api/ai-proxy` returns streaming responses via Vercel AI SDK.
2. All existing AI providers (OpenAI, Anthropic, Google) continue to work.
3. Code complexity in `ai-proxy/route.ts` is significantly reduced.
</success_criteria>

<output>
After completion, create `.planning/phases/phase-06/phase-06-02-SUMMARY.md`
</output>
