---
phase: phase-06
plan: 05
type: execute
wave: 5
depends_on: [06-04-PLAN.md]
files_modified: [src/ai/flows/ai-deck-coach-review.ts, e2e/ai-streaming.spec.ts]
autonomous: true
requirements: [REQ-T6, REQ-T5]
must_haves:
  truths:
    - AI Coach evaluation falls back to heuristic mode when offline.
    - E2E tests verify streaming, multi-provider, and tool-calling support.
  artifacts:
    - path: e2e/ai-streaming.spec.ts
      provides: Comprehensive E2E tests for AI streaming and MCP tools.
      min_lines: 100
  key_links:
    - from: e2e/ai-streaming.spec.ts
      to: src/app/api/chat/route.ts
      via: direct HTTP/SSE requests
---

<objective>
Implement offline fallback and verify Phase 6 with E2E tests.

Purpose: Ensure the system is robust against network outages and all success criteria are met.
Output: Integrated heuristic fallback for AI Coach and a suite of verification tests.
</objective>

<execution_context>
@/home/alex/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/phase-06/CONTEXT.md
@src/ai/flows/ai-deck-coach-review.ts
@e2e/ai-coach.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Heuristic Fallback for AI Coach</name>
  <files>src/ai/flows/ai-deck-coach-review.ts</files>
  <action>
    Modify the AI Coach logic to detect network availability or API errors:
    - Use browser `navigator.onLine` and API response status codes.
    - Fallback to the existing heuristic-based evaluation when the LLM is unavailable.
    - Provide a clear UI message to the user indicating they are in "Heuristic Mode (Offline)".
  </action>
  <verify>
    <automated>npx vitest src/ai/flows/ai-deck-coach-review.test.ts</automated>
  </verify>
  <done>Heuristic fallback implemented and correctly activates when needed.</done>
</task>

<task type="auto">
  <name>Task 2: Create AI Streaming E2E Tests</name>
  <files>e2e/ai-streaming.spec.ts</files>
  <action>
    Implement Playwright tests to verify:
    - Multi-provider switching in settings.
    - SSE streaming (checking for multiple chunks).
    - Offline fallback by mocking failed API calls.
    - Tool calling (MCP bridge) returns valid card data in the chat.
  </action>
  <verify>
    <automated>npx playwright test e2e/ai-streaming.spec.ts</automated>
  </verify>
  <done>Comprehensive E2E tests ensure all Phase 6 requirements are satisfied.</done>
</task>

</tasks>

<verification>
Run the new E2E tests and ensure they all pass. Manually test offline mode by disabling the internet and using the AI Coach.
</verification>

<success_criteria>
1. AI Coach remains functional (with reduced intelligence) when offline.
2. E2E tests pass for all major LLM providers.
3. Streaming performance (REQ-T5) is met (initial chunk < 500ms).
</success_criteria>

<output>
After completion, create `.planning/phases/phase-06/phase-06-05-SUMMARY.md`
</output>
