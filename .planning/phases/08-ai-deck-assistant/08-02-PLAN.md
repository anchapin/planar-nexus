---
phase: 08-ai-deck-assistant
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified: [src/app/(app)/deck-builder/_components/ai-deck-assistant.tsx, src/app/(app)/deck-builder/page.tsx]
autonomous: true
requirements: ["REQ-11.1", "REQ-11.2"]
must_haves:
  truths:
    - "AIDeckAssistant renders 5 card suggestions with scores and confidence"
    - "Users can add suggestions to their deck with a single click"
    - "Clicking 'Why this card?' initiates a streamed AI explanation"
  artifacts:
    - path: "src/app/(app)/deck-builder/_components/ai-deck-assistant.tsx"
      provides: "AI Assistant UI and streaming integration"
  key_links:
    - from: "src/app/(app)/deck-builder/_components/ai-deck-assistant.tsx"
      to: "/api/chat"
      via: "streamText (Vercel AI SDK)"
    - from: "src/app/(app)/deck-builder/_components/ai-deck-assistant.tsx"
      to: "src/app/(app)/deck-builder/_components/synergy-context.tsx"
      via: "useSynergy hook"
---

<objective>
Implement the AI Deck Assistant user interface, enabling users to see card suggestions and request streamed AI explanations for those suggestions.

Purpose: To provide proactive, explained deck-building assistance.
Output: AIDeckAssistant component integrated into the deck builder.
</objective>

<execution_context>
@/home/alex/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/08-ai-deck-assistant/08-01-SUMMARY.md
@src/app/(app)/deck-builder/_components/synergy-context.tsx
@src/app/api/chat/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AIDeckAssistant Component</name>
  <files>src/app/(app)/deck-builder/_components/ai-deck-assistant.tsx</files>
  <action>
    Develop the `AIDeckAssistant` UI component:
    1. Consume `useSynergy` to access `topSuggestions` and `synergyData`.
    2. Render a list of 5 suggestions in a side-panel or card within the deck builder.
    3. For each card, display its name, synergy score (e.g., "Synergy: 85%"), and confidence badge (High/Medium/Low).
    4. Implement an "Add to Deck" button for each suggestion.
    5. Include a "Why this card?" button that triggers an explanation flow.
    
    Styling: Use Shadcn/UI (Card, Button, Badge, Progress) for consistency.
  </action>
  <verify>
    <automated>Verify the component file exists and exports AIDeckAssistant.</automated>
  </verify>
  <done>AIDeckAssistant component displays suggestions with scores and confidence.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Streamed Explanations</name>
  <files>src/app/(app)/deck-builder/_components/ai-deck-assistant.tsx</files>
  <action>
    Implement synergy explanation functionality in `AIDeckAssistant`:
    1. Use the Vercel AI SDK `useChat` or `streamText` patterns to call `/api/chat`.
    2. When "Why this card?" is clicked:
       - Construct a prompt: "Explain the synergy between the card '{cardName}' and the current deck which includes: {deckCardNames}. Focus on mechanical synergy and strategy."
       - Display the streamed explanation in a modal or inline collapsible section.
       - Use a typing indicator while waiting for the first chunk.
    3. Ensure error handling for API failures.
  </action>
  <verify>
    <automated>Verify /api/chat is being called with the correct prompt on button click (can use mock tests if available).</automated>
  </verify>
  <done>AI Assistant provides real-time, streamed explanations for synergistic cards.</done>
</task>

<task type="auto">
  <name>Task 3: Main Page Integration</name>
  <files>src/app/(app)/deck-builder/page.tsx</files>
  <action>
    Integrate `SynergyProvider` and `AIDeckAssistant` into `DeckBuilderPage`:
    1. Wrap the entire `DeckBuilderPage` content in `SynergyProvider`, passing the current `deck` state.
    2. Place `AIDeckAssistant` in the layout (e.g., as a new column or a toggleable sidebar on the right).
    3. Connect the "Add to Deck" callbacks.
  </action>
  <verify>
    <automated>Check if SynergyProvider and AIDeckAssistant are imported and used in deck-builder/page.tsx.</automated>
  </verify>
  <done>The deck builder page now features the AI Deck Assistant.</done>
</task>

</tasks>

<verification>
Manually verify that suggestions update as cards are added, and that clicking "Why this card?" results in a streaming response from the AI.
</verification>

<success_criteria>
1. Assistant correctly shows 5 suggestions not present in the deck.
2. Synergy scores and confidence levels are clearly visible.
3. AI explanations stream smoothly into the UI.
</success_criteria>

<output>
After completion, create `.planning/phases/08-ai-deck-assistant/08-02-SUMMARY.md`
</output>
