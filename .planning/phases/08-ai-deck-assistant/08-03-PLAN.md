---
phase: 08-ai-deck-assistant
plan: 03
type: execute
wave: 3
depends_on: ["08-01", "08-02"]
files_modified: [src/app/(app)/deck-builder/_components/card-search.tsx, e2e/ai-deck-assistant.spec.ts]
autonomous: true
requirements: ["REQ-11.1", "REQ-11.2"]
must_haves:
  truths:
    - "Synergy scores are overlaid on relevant cards in search results"
    - "The E2E test suite successfully simulates a full deck-building session with AI help"
    - "High-synergy cards are visually distinct from low-synergy ones"
  artifacts:
    - path: "e2e/ai-deck-assistant.spec.ts"
      provides: "Full coverage of Phase 8 features"
  key_links:
    - from: "src/app/(app)/deck-builder/_components/card-search.tsx"
      to: "src/app/(app)/deck-builder/_components/synergy-context.tsx"
      via: "useSynergy hook"
---

<objective>
Enhance the card search experience by highlighting synergistic cards and provide comprehensive end-to-end verification of the AI Assistant features.

Purpose: To visually surface synergies across all search results and ensure quality through testing.
Output: Enhanced CardSearch UI and E2E test suite.
</objective>

<execution_context>
@/home/alex/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@src/app/(app)/deck-builder/_components/card-search.tsx
@src/app/(app)/deck-builder/_components/synergy-context.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Highlight Synergy in CardSearch</name>
  <files>src/app/(app)/deck-builder/_components/card-search.tsx</files>
  <action>
    Modify `CardSearch` to visually display synergy scores:
    1. Consume `useSynergy` to access `synergyData`.
    2. For each card in the search results:
       - Check if it has a synergy entry in `synergyData`.
       - If its synergy score is above a threshold (e.g., 60%), overlay a subtle "Synergy Score" badge or ring on the card image.
       - Use color coding: Green for High (80%+), Yellow for Medium (60%+).
    3. Ensure no performance regression by using `memo` on card components or optimizing context consumption.
  </action>
  <verify>
    <automated>Verify the card rendering logic uses data from the synergy context.</automated>
  </verify>
  <done>Synergistic cards are visually highlighted within search results.</done>
</task>

<task type="auto">
  <name>Task 2: Add E2E Tests for AI Assistant</name>
  <files>e2e/ai-deck-assistant.spec.ts</files>
  <action>
    Create a new Playwright E2E test file:
    1. Test deck builder initial load (assistant should be empty or have generic suggestions).
    2. Add specific cards to the deck (e.g., "Sol Ring", "Arcane Signet").
    3. Verify that the assistant updates with synergistic suggestions.
    4. Click "Why this card?" and assert that a streamed response appears.
    5. Verify synergy badges appear on card search results.
  </action>
  <verify>
    <automated>npx playwright test e2e/ai-deck-assistant.spec.ts</automated>
  </verify>
  <done>AI Deck Assistant features are fully covered by E2E tests.</done>
</task>

</tasks>

<verification>
Run all E2E tests to confirm functionality and manually audit the UI for performance during search.
</verification>

<success_criteria>
1. All card search results with high synergy are visibly marked.
2. Playwright tests pass for suggestions and explanations.
3. No noticeable UI lag (maintaining 60fps) during deck updates and search.
</success_criteria>

<output>
After completion, create `.planning/phases/08-ai-deck-assistant/08-03-SUMMARY.md`
</output>
