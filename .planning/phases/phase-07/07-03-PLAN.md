---
phase: 7
plan: 3
title: Local Search Engine (Orama)
depends_on: ["07-02"]
must_haves:
  - Orama index with hybrid schema (text + vector).
  - Indexing logic for card data and embeddings.
---

# Plan 07-03: Local Search Engine (Orama)

Set up the hybrid search index and implement core indexing logic.

<task id="07-03-01" title="Initialize Orama Schema">
<files>
  <file>src/lib/search/orama-manager.ts</file>
</files>
<action>
Create `src/lib/search/orama-manager.ts`:
- Define the Orama schema: `id`, `name`, `text`, `type`, `color`, `vector` (384-dimensional for `all-MiniLM-L6-v2`).
- Set up a hybrid index (BM25 for text + cosine similarity for vectors).
- Export an `OramaManager` class that manages an internal Orama instance.
</action>
<verify>
Check the schema definition and index configuration for hybrid search support.
</verify>
<done>
Orama instance with proper schema is initialized.
</done>
</task>

<task id="07-03-02" title="Implement Indexing Methods">
<files>
  <file>src/lib/search/orama-manager.ts</file>
</files>
<action>
In `OramaManager`:
- Implement `upsertCards(cards, embeddings)`:
  - Format card data into Orama-compatible documents.
  - Efficiently insert documents into the Orama index.
- Implement basic `search(params)`: Handle text queries, vector similarity, and property-based filtering.
</action>
<verify>
Check the `upsertCards` implementation for proper mapping of card data and embeddings.
</verify>
<done>
Orama indexing methods are ready for use.
</done>
</task>
