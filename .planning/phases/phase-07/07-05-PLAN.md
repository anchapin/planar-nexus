---
phase: 7
plan: 5
title: UI Integration & Verification
depends_on: ["07-04"]
must_haves:
  - `useLocalSearch` hook for components.
  - Progress indicator for first-run indexing.
  - Performance and correctness tests.
---

# Plan 07-05: UI Integration & Verification

Expose the local search capabilities to the UI and verify that all success criteria are met.

<task id="07-05-01" title="Create `useLocalSearch` Hook">
<files>
  <file>src/hooks/use-local-search.ts</file>
</files>
<action>
Create `src/hooks/use-local-search.ts`:
- Implement a React hook that wraps `OramaManager.search`.
- Support:
  - Text search (fuzzy).
  - Vector search (semantic similarity).
  - Property filters (colors, type, etc.).
  - Debouncing to optimize performance.
- Return search results, loading state, and the `OramaManager` status.
</action>
<verify>
Confirm the hook correctly exposes the search API and handles async results.
</verify>
<done>
`useLocalSearch` hook is ready for UI components.
</done>
</task>

<task id="07-05-02" title="Add Indexing Progress Component">
<files>
  <file>src/components/search/IndexingProgress.tsx</file>
</files>
<action>
Create `src/components/search/IndexingProgress.tsx`:
- Listen to `BackgroundIndexingManager` events (progress, status).
- Show a progress bar or status message during the first-run embedding and indexing process.
- Ensure the UI remains responsive and provides feedback to the user.
</action>
<verify>
Confirm the progress UI updates in real-time as indexing progresses.
</verify>
<done>
Indexing progress indicator is implemented.
</done>
</task>

<task id="07-05-03" title="Verification & Performance Tests">
<files>
  <file>src/lib/search/orama-manager.test.ts</file>
  <file>src/lib/ai/embedding-worker.test.ts</file>
</files>
<action>
Implement automated tests:
- **Search Latency**: Measure time taken for vector searches. Success: < 50ms.
- **Correctness**: Verify that semantic search returns logically similar cards (e.g., searching for "draw card" returns cards with that mechanic).
- **Persistence**: Verify that the index survives page reloads.
- **CPU Usage**: Manually monitor CPU usage during a test indexing run to ensure it stays below 20%.
- **WebGPU Fallback**: Verify that the system falls back to WASM if WebGPU is unavailable.
</action>
<verify>
Run the tests and confirm all pass and meet success criteria.
</verify>
<done>
Phase 7 is verified and meets all goals.
</done>
</task>
