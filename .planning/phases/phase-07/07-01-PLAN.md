---
phase: 7
plan: 1
title: Infrastructure & Core Libraries
depends_on: []
must_haves:
  - Core search and embedding libraries installed.
  - Dexie.js database schema defined for embeddings and history.
---

# Plan 07-01: Infrastructure & Core Libraries

Establish the foundational libraries and data storage schema for the local intelligence engine.

<task id="07-01-01" title="Install Core Dependencies">
<files>
  <file>package.json</file>
</files>
<action>
Install the following libraries:
- `@orama/orama` (v3+)
- `@orama/plugin-data-persistence`
- `@huggingface/transformers` (v3+)
- `dexie` (v4+)
- `dexie-react-hooks`
</action>
<verify>
Check `package.json` for the new dependencies.
</verify>
<done>
Dependencies are present in `package.json`.
</done>
</task>

<task id="07-01-02" title="Define Dexie.js Schema">
<files>
  <file>src/lib/db/local-intelligence-db.ts</file>
</files>
<action>
Create a new file `src/lib/db/local-intelligence-db.ts`:
- Define a `LocalIntelligenceDB` class extending `Dexie`.
- Define tables:
  - `embeddings`: `++id, cardId, [cardId+model], vector`
  - `orama_snapshots`: `id, data, timestamp`
  - `game_history`: `++id, timestamp, type, data`
  - `player_decisions`: `++id, gameId, timestamp, decision, context`
- Export a singleton `db` instance.
</action>
<verify>
Confirm the file exists and correctly defines the schema for embeddings, snapshots, and game history (REQ-10.3).
</verify>
<done>
`src/lib/db/local-intelligence-db.ts` is created and exported.
</done>
</task>
