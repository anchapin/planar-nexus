---
phase: 7
plan: 2
title: Embedding Generation (Transformers.js)
depends_on: ["07-01"]
must_haves:
  - Singleton for Transformers.js pipeline.
  - Web Worker for off-thread inference.
  - Card-to-text transformation strategy.
---

# Plan 07-02: Embedding Generation (Transformers.js)

Implement off-main-thread embedding generation with WebGPU support and WASM fallback.

<task id="07-02-01" title="Implement Transformers.js Singleton">
<files>
  <file>src/lib/ai/transformers-singleton.ts</file>
</files>
<action>
Create `src/lib/ai/transformers-singleton.ts`:
- Define `PipelineSingleton` with the `all-MiniLM-L6-v2` model.
- Configure `device: 'webgpu'` with a catch-and-retry for `device: 'wasm'`.
- Implement `getInstance(progress_callback)` to ensure only one instance of the pipeline exists.
</action>
<verify>
Check the singleton implementation for WebGPU/WASM fallback logic.
</verify>
<done>
Transformers.js singleton is implemented and ready for worker use.
</done>
</task>

<task id="07-02-02" title="Define Card-to-Text Strategy">
<files>
  <file>src/lib/ai/card-text-processor.ts</file>
</files>
<action>
Create `src/lib/ai/card-text-processor.ts`:
- Implement `formatCardForEmbedding(card)`: Combine name, types, mana cost, and rules text into a structured, semantically meaningful string.
- Example: "Card: [Name]. Type: [Types]. Cost: [ManaCost]. Rules: [RulesText]."
- Ensure this captures synergies (e.g., keywords, mechanics).
</action>
<verify>
Confirm the text transformation logic is consistent and captures all relevant card fields.
</verify>
<done>
Card text processor is implemented.
</done>
</task>

<task id="07-02-03" title="Implement Embedding Web Worker">
<files>
  <file>src/lib/ai/embedding-worker.ts</file>
  <file>src/lib/ai/embedding-client.ts</file>
</files>
<action>
- Create `src/lib/ai/embedding-worker.ts`: Listens for `GENERATE_EMBEDDINGS` and uses `PipelineSingleton` to process batches of cards.
- Create `src/lib/ai/embedding-client.ts`: Exports a singleton `EmbeddingClient` that manages worker communication via `postMessage`.
- Handle batching (50-100 cards) and pass progress reporting messages.
</action>
<verify>
Ensure the worker is correctly instantiated and handles message passing without blocking the main thread.
</verify>
<done>
Embedding worker and client are fully functional.
</done>
</task>
