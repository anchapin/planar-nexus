---
phase: 18-meta-analysis-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: ["META-01", "META-02", "META-03", "META-04"]
user_setup: []

must_haves:
  truths:
    - "User can select format (Standard/Modern/Commander) and see top deck archetypes"
    - "User can view win rates and meta share percentages for each deck"
    - "User can see card inclusion rates in top decks"
    - "User can filter meta data by date range (7 days, 30 days, all time)"
  artifacts:
    - path: "src/app/(app)/meta/page.tsx"
      provides: "Meta analysis dashboard page"
      min_lines: 50
    - path: "src/lib/meta.ts"
      provides: "Meta data types, mock data, and utilities"
      min_lines: 100
      exports: ["DeckArchetype", "MetaData", "getMetaData", "getTopDecks", "getCardInclusionRates"]
    - path: "src/components/meta/MetaOverview.tsx"
      provides: "Main meta dashboard component with format selector"
      min_lines: 50
    - path: "src/components/meta/DeckArchetypeCard.tsx"
      provides: "Card showing deck archetype with win rate and meta share"
      min_lines: 30
---

<objective>
Build meta deck analysis UI showing top deck archetypes, win rates, meta share, and card inclusion rates with date filtering.

Purpose: Enable users to view competitive deck landscape in their selected format.
Output: Meta dashboard page with deck archetype cards and inclusion rate charts.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@/home/alex/Projects/planar-nexus/src/app/actions.ts
@/home/alex/Projects/planar-nexus/src/lib/game-rules.ts

Reference: Existing dashboard and deck-builder routes for component patterns.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create meta data types and mock data</name>
  <files>src/lib/meta.ts</files>
  <action>
Create src/lib/meta.ts with:
- TypeScript interfaces for DeckArchetype, MetaData, CardInclusionRate
- Mock data for Standard, Modern, Commander formats
- Sample deck archetypes (5-10 per format) with realistic names, win rates, meta share %
- Helper functions: getMetaData(format, dateRange), getTopDecks(format), getCardInclusionRates(archetypeId)
- Use meaningful card names from Magic: The Gathering for authenticity
  </action>
  <verify>tsc --noEmit passes</verify>
  <done>Meta types and mock data exported and usable</done>
</task>

<task type="auto">
  <name>Task 2: Create MetaOverview component</name>
  <files>src/components/meta/MetaOverview.tsx</files>
  <action>
Create src/components/meta/MetaOverview.tsx:
- Format selector (Standard, Modern, Commander tabs)
- Date range filter dropdown (7 days, 30 days, all time)
- Grid of DeckArchetypeCard components
- Responsive layout using existing Tailwind/shadcn patterns
- Use Card, Select, Tabs components from shadcn/ui
  </action>
  <verify>npm run build succeeds</verify>
  <done>MetaOverview component renders with format selection and date filtering</done>
</task>

<task type="auto">
  <name>Task 3: Create DeckArchetypeCard component</name>
  <files>src/components/meta/DeckArchetypeCard.tsx</files>
  <action>
Create src/components/meta/DeckArchetypeCard.tsx:
- Display deck name, archetype (aggro, control, midrange, combo)
- Win rate percentage with color coding (green >55%, yellow 45-55%, red <45%)
- Meta share percentage bar
- Card inclusion rate mini-chart (top 10 cards)
- Click to expand showing full card list
  </action>
  <verify>npm run build succeeds</verify>
  <done>DeckArchetypeCard displays all required information</done>
</task>

<task type="auto">
  <name>Task 4: Create meta page route</name>
  <files>src/app/(app)/meta/page.tsx</files>
  <action>
Create src/app/(app)/meta/page.tsx:
- Import MetaOverview component
- Add to layout with page title "Meta Analysis"
- Use existing layout structure from other routes
- Ensure proper client component handling
  </action>
  <verify>npm run build succeeds</verify>
  <done>Meta page accessible at /meta</done>
</task>

</tasks>

<verification>
- [ ] npm run build succeeds
- [ ] npm run typecheck passes
- [ ] /meta route renders without errors
- [ ] Format selector switches between Standard/Modern/Commander
- [ ] Date range filter updates displayed data
- [ ] Deck archetype cards show win rate and meta share
</verification>

<success_criteria>
- All tasks completed
- Meta dashboard displays with mock data
- Format selection works
- Date filtering works
- No build errors or warnings
</success_criteria>

<output>
After completion, create `.planning/phases/18-meta-analysis-foundation/18-01-SUMMARY.md`
</output>
